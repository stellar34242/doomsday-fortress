# 待开发记录

> 本文档集中维护全部待开发项，按序号列出，**不记录版本号**；各编辑器域的设计细节见对应设计文档
> （《堡垒编辑器设计》《炮塔编辑器设计》（弹丸域已并入）《战场编辑器设计》）。
> 序号为稳定 ID：已完成或删除的条目直接移除并保留空缺，不重新编号。
> 最近更新：2026-08-22。

---

## 3. 底盘家族扩展
- **半履带**：前轮换向 + 后履带差速，求解器同输前轮角+差速；现有架构已铺底（履带/轮子美术组可共存、统一落印列）。**工作量：中小**
- **气垫**：低摩擦滑行、大惯性、转向即甩尾；以简化惯性模型实现（原漂移模型方案已取消）。**工作量：中**
- **铰接式**：双节车体、以铰接夹角转向；涉及双刚体运动学、双矩形碰撞与渲染。**工作量：中~大**
- **多轴**：多组轮轴、多轴转向（长车体小转弯半径）；轮式自行车模型的多轴推广。**工作量：中小**
- **步行**：足式底盘，全向移动、原地自由转向、强越障；步态动画与落地印迹另设。**工作量：中~大**

## 4. 预留索敌标签键激活
- 堡垒优先/僚机优先/导弹优先/生产单位优先等键的类型位已预留；对应实体（敌方堡垒、僚机、可拦截导弹、生产单位）上线后合流激活即可
- **工作量：小**（每键数行；实体系统本体为各自独立大特性，不计入本项）

## 6. 驾驶员控制模式
- 驾驶员为**全新轻量单位类型**（并非战车，战车参数体系不适用）：步行移动、上下车交互、战车被毁后登入空战车续战
- **与现有毁灭序列的判负联动**：驾驶员未下车时战车结构归零 → 驾驶员同亡、对局结束；已下车后战车才毁 → 不被主爆 AOE（伤害 25，半径 max(w,h)×0.8）波及则对局继续
- **禁止下车开关**：部分单机关卡与对战模式禁用下车，作为关卡/对局配置项
- **工作量：大**（新单位类型 + 操控切换 + 与敌人索敌/碰撞系统的交互矩阵）

## 7. 装备解锁系统（单机先行已确认）
- 单机关卡通关解锁战车车体/炮塔/模块；解锁数据格式先行落地（装备标记解锁来源关卡 + 解锁状态存档），联机模式将来读取同一份数据做准入校验
- **工作量：中**

## 8. 多人联机模式
- 被解锁系统引用为准入校验场景；网络同步/房间/对局规则均为空白
- **工作量：特大，独立专项**（未立项）

## 11. 触屏优化
- 操控/摇杆层改动面大
- **工作量：中**

## 17. 统一界面体系（操作台式主界面 + 整备界面 + 战斗内弹窗）
- **主界面方向已拍板：模块化操作台**——底部不再只是平铺文字与进度条，而由若干可组合的显示屏、仪表和控制模块组成，整体呈现驾驶员操作台/车载控制台的感觉。
- **防护状态显示屏**：以车体方向图展示前/后/左/右装甲的当前值与上限，并整合护盾和结构值；支持受击面闪烁、格挡反馈、破甲/破盾状态和低结构警报。
- **资源与武器显示模块**：弹药仓显示总弹药、消耗趋势与短缺提示；独立模块显示电量、热量和散热状态；后续可按选中炮塔展示弹种、装填与开火状态。
- **操作按钮模块**：炮塔、模块、驾驶/交互等按钮采用统一的实体控制台视觉与按压反馈；按钮可根据当前模式、装备解锁和资源状态禁用或告警。
- **布局原则**：各 UI 模块拥有统一外壳、标题、状态色和数据接口，可按横屏/竖屏重新编排；信息层级优先保证战斗可读性，不在战场画面上常驻绘制装甲边框。
- **共用面板体系**：**整备界面**（战车装配/炮塔/模块管理）与 **战斗中点击炮塔/模块按钮的弹出窗口** 共用查看、装配和参数展示组件，保持交互范式一致。
- 边界（已澄清）：整备界面与关卡任务界面（#18）**并非同一套面板**——任务界面只从**已预设战车**中选择出战，不做现场装配
- 联动：现有四向装甲与护盾、#7 装备解锁（置灰与准入）、#20 涂装选择 tab
- **工作量：大**（主 HUD 模块化 + 响应式操作台布局 + 整备/战斗面板组件统一）

## 18. 剧情与关卡流程界面链
- **关卡流程三界面（已拍板）**：
  1. **任务介绍界面**（进关前）：任务简报 + 目标说明（对接 #22 objective）+ 出战战车选择——**只能从已预设（预先装配好）的战车中选一辆，本界面不做装配**（装配在整备界面，见 #17）
  2. **剧情演出**：进入关卡时与通关时均可配置剧情演出，走引擎内演出
  3. **结算界面**（通关后）：击杀统计、获得奖励（新装备/涂装/徽记，对接 #7 解锁发放）等
- 已拍板：关卡内对话与引擎内演出**合并为同一系统**——对话、镜头移动、单位走位都是脚本轨道上的事件（Beat 数组：文本/对话/镜头/单位移动等类型可扩展）；对话皮肤 = 侧边电台通讯栏为主 + 单位头顶气泡兜底（短句喊话）
- 触发：复用现有关卡时间、波次与区域事件入口，接入剧情演出脚本轨道
- **工作量：中大**（脚本运行时是新地基 + 三个界面骨架；视觉零素材依赖）

## 20. 涂装与装饰扩展
- **待开发：强调色扩展**——目前 `accent` 仅用于车头三角方向标；扩展到车身饰条、舱盖边框、灯具外壳和炮位环等部位。采用可配置的程序化强调区域/锚点（部位类型、位置、尺寸、角度），编辑器外观预览与战场共用同一绘制函数；不染色履带、轮子、炮塔、徽记和四向装甲战斗逻辑。若需要给主体贴图内部的特定纹理换色，再追加独立强调色遮罩素材，不按整张主体贴图染色。
- **可选扩展（未拍板）**：程序化迷彩、磨损旧化。
- **与 #7 联动**：新涂装和徽记作为关卡解锁奖励。
- **工作量：小~中**（强调区域数据、编辑器控件、共用渲染与兼容校验）

## 23. 护盾视觉参数调优
- **状态说明**：圆角矩形护盾、常态内部场、边缘能量流、单次受击六边形扩散、开展和破盾碎片均已实现；本项只记录后续视觉调参，不代表护盾功能尚未完成。
- **形状与铺设（当前基准）**：圆角半径为护盾短半边的 `38%`；护盾比车体外扩 `max(6px, 单格尺寸×0.22)`；受击六边形尺寸为 `max(13px, 单格尺寸×0.72)`。
- **内部场体（当前基准）**：底色 `rgb(76,190,207)`，基础透明度随剩余护盾由 `2.2%` 增至 `3.8%`；场体素材双层反向漂移，第一层透明度系数约 `0.62~0.90`，第二层约 `0.46~0.70`，并带小幅位移和旋转。
- **常态边缘（当前基准）**：边缘主线 `rgb(225,255,255)`，基础透明度随剩余护盾为 `34%~46%`；亮度采用双频呼吸，约在基准值的 `±15.5%` 内变化，线宽约 `±6.5%` 变化；柔光色为 `#B7F4F7`。
- **沿边能量波峰（当前基准，已回到初版）**：每圈约 `6.67 秒`，波头宽度参数 `0.055`、拖尾宽度 `0.11`、拖尾间距 `0.13 圈`；波峰只调制原有边缘柔光采样点的亮度（增益基线 `0.82`、波头最高增加 `0.72`、拖尾最高增加 `0.16`），不再叠加独立高亮光带或亮色描线。
- **单次受击（当前基准）**：六边形按“命中格 → 紧邻一圈 → 外围第二圈”播放一次扩散；三层峰值亮度系数依次为 `1.00 / 0.62 / 0.34`。正常状态每圈延迟为受击事件进度的 `0.14`、单圈脉冲持续 `0.26`；破盾状态对应为 `0.12 / 0.34`，播放结束后全部隐藏。
- **护盾开展（当前基准）**：总时长 `0.82 秒`；从 `50%` 尺寸、`6%` 透明度开始平滑开展；末段轻微回弹，最大额外缩放约 `1.8%`；开展期间边缘亮度最高额外增强 `70%`。
- **破盾（当前基准，整体柔性崩解版）**：命中位置只负责最初的亮核、电弧和六边形波纹，护盾本体不从命中点向外传播破碎。内部场体在 `0.26 秒` 内整体淡出并最多收束约 `8%`；完整外缘共同失稳，各段只在 `0.08 秒` 内做随机错峰并各自淡出约 `0.16 秒`。碎片发射点以约 `0.72` 世界单位间距的交错网格**平均覆盖整个圆角矩形护盾范围**（包括中心与内部区域），并确定性剔除 `60%` 采样点，使主碎片总量约为完整采样方案的 `2/5`；所有区域在 `0.12 秒` 内随机错峰释放，每点 `2~3` 片。主碎片为**无描边的不规则三角形、四边形、狭长条和锐角半透明能量薄片，不使用六边形造型**，分为 `30%` 大型缓慢、`50%` 标准、`20%` 狭长高速三类，并带淡青/近白青/深青色差和少量初生亮度增强；基础尺寸为护盾短边的 `7%`（限制 `0.20~0.60` 世界单位），保留 `±42%` 单片随机差异，基准速度 `3.5`、寿命 `1.05 秒`，飞散期间每秒缩小约 `8%`。独立冰晶层按每片主碎片 `45%` 的确定性概率生成，长期总量约为主碎片的 `45%`；尺寸为主碎片的 `24%`、初速 `5.4`、寿命约 `1 秒`（`±18%`），阻力 `6.5`，表现为高速起步后快速减速漂浮，采用近白青细长锐角造型和轻微闪烁。整体运动方向混合径向与较强切向分量，并穿插少量短命余辉粒子。
- **后续重点调整**：圆角程度；内部场体的可见度与动态幅度；边缘呼吸幅度/频率；能量波峰的方向、速度、长度和对比度；受击扩散节奏；破盾闪光强度、碎片密度/尺寸/寿命。当前数值均为临时视觉基准，确定最终风格后再固化；若需要多种护盾外观，应将这些数值移入可配置的护盾视觉预设，而不是继续写死在渲染代码中。
- **工作量：小~中**（单纯调参为小；做成护盾视觉预设与编辑器参数为中）
