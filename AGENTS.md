# 多任务开发协作规则

本仓库可能被多个 Codex 任务同时修改。凡是会写入代码、素材、配置或文档的开发任务，都必须遵守下列同步协议。

## 开始开发前

1. 完整阅读 `开发同步/README.md`。
2. 检查并阅读 `开发同步/进行中/` 下除 `_说明.md` 之外的全部 `.md` 文件。
3. 在修改任何项目文件前，为当前任务新建一份独立登记文件。文件名使用：
   `YYYYMMDD-HHmm-任务简称-任务ID后8位.md`。
4. 登记文件至少写明：任务、任务 ID、状态、开始时间、最后更新时间、工作范围、计划修改文件、当前进展、冲突提醒。

纯读取、解释、评估且不会修改仓库内容的任务不需要登记。

## 开发过程中

- 每个任务只能编辑和删除自己的登记文件，禁止改写或删除其他任务的登记。
- 扩大工作范围或准备修改新的文件前，先更新自己的“工作范围”和“计划修改文件”。
- 如果与其他进行中任务的功能范围或文件重叠，先停止重叠部分的修改，向用户说明冲突；可以继续推进确认不重叠的部分。
- 不得因为其他任务的改动导致检查失败，就擅自回滚、覆盖或修复对方正在修改的文件。
- 暂停等待用户输入时保留登记，并将状态改为“等待用户”；恢复后改回“开发中”。
- 发现看似遗留的登记时不要自行删除。把它视为潜在活动任务，先通过任务状态或用户确认其是否已经结束。

## 完成开发后

1. 完成必要的检查、测试和文档更新。
2. 确认没有待办工作后，只删除当前任务自己的登记文件。
3. 在最终回复中说明登记已经清理。

规则文件 `AGENTS.md`、`开发同步/README.md` 和 `开发同步/进行中/_说明.md` 为永久文件，不属于临时登记，任何任务不得在完成时删除它们。
