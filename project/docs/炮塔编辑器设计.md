# 炮塔编辑器设计文档

> 本文档为**炮塔与弹丸域**的完整设计文档：按系统结构组织，仅描述现行设计，不含版本变更记录。
> 弹丸域内容已并入本文档（原《弹丸编辑器设计》文档废止，弹丸系统见第 7 章、弹丸编辑器见第 12 章）。
> **负责范围**：炮塔定义（TurretDef）与类型体系、索敌系统、开火行为与资源、直射/抛射/导弹/射线/喷射五类武器、
> 弹丸库（ProjectileArtDef）、命中效果、炮塔美术与渲染分层、炮塔/弹丸编辑器 UI、口令与持久化。
> **姊妹文档**：《堡垒编辑器设计》（炮位/挂载/堡垒机动）、《战场编辑器设计》（战场/主界面布局）。
> **待开发项**：统一维护于《待开发记录》（按序号、不记版本号）。

---

## 1. 总览与类型体系

### 1.1 炮塔域范围

炮塔域负责炮塔定义（`TurretDef` 数值参数、型号 S/M/L、导弹制导参数）、美术配置（`art`：底座/炮身/炮管/火光/充能/后坐/弹丸引用）与弹丸美术库（`ProjectileArtDef`）。所有出厂定义集中在 `src/game/config.ts`：`TURRET_DEFS` 为可变注册表（内存编辑 + bump，不持久化于代码外），弹丸侧对应 `PROJECTILE_ARTS` 注册表。

### 1.2 炮塔五类（WeaponType）

代码口径：`type WeaponType = 'direct' | 'lob' | 'missile' | 'beam' | 'spray'`。类型决定弹道模型、可用参数子集与弹丸类别映射。

| type | 中文名 | 定位与弹道模型 | 可用特征参数 | 出厂代表炮塔（TURRET_DEFS） |
|---|---|---|---|---|
| `direct` | 直射 | 实弹直线飞行，按弹速推进，可穿透、可爆炸（`blastRadius>0` 时命中点波及） | projectileSpeed、accuracy、pierce、blastRadius/blastEffect | 哨戒机枪（mg，S 型双管轮流 6 连发）、加农炮（M）、大型双联加农炮（L 双管齐射） |
| `lob` | 抛射 | 抛物线弹道，按飞行时间插值，落点爆炸 + 燃烧 | projectileSpeed（缺省 60）、blastRadius（落点爆炸缺省 15m）、blastEffect.burn | 榴弹抛射炮（lob，M 型，爆炸 20m + 命中燃烧） |
| `missile` | 导弹 | 自驱动弹道：初速→加速爬升→（可选延迟制导/蛇形/燃尽滑行/集束分裂），可制导追踪、可对空 | guided、guideDelay/guideDecel、missileInitSpeed/Accel/MaxSpeed/TurnMax/TurnAccel、missileFlightTime、missileCurve、ejectAngle、burnTime、split | 巡航导弹井（cruise，L 型非制导锁定落点）、猎手制导导弹（hunter，S 型 4 管轮流、延迟制导、可对空） |
| `beam` | 射线 | 无实体弹丸的即时命中类；由 `rayMode` 分子模式：`'beam'` 宽幅矩形持续 tick（发射期间炮塔转向速度 ×0.5 削减）、`'pulse'` 点射（单体即中、不可穿透、发射期间正常转向） | rayMode、beamWidth（仅光束）、attackDuration/dot（仅光束）、chargeTime、energyPerShot/energyPerSec | 磁轨光束塔（beam，rayMode='beam'，持续伤害 + 充能前摇）、脉冲射线塔（pulse，rayMode='pulse'，按发耗电） |
| `spray` | 喷射 | 扇形火焰锥持续灼烧，无弹丸（`art.projectile` 配置不生效），按秒耗弹药 | sprayAngle、attackDuration、dot、reload、ammoPerSec | 烈焰喷射塔（spray，S 型 60° 扇形） |

> 注：不存在近战（melee）/生产（produce）炮塔类型；「生产」仅存在于堡垒内部模块（`ModuleDef.produce`）与特殊格加成类别（`SpecialBoost` 含 'produce'），不属于炮塔域。

### 1.3 弹丸/光束美术定义（ProjectileArtDef）的定位

弹丸库（`PROJECTILE_ARTS`）是与 `TURRET_DEFS` 同模式的可变注册表，承载**纯表现层**配置：弹体贴图、尾焰（trail）、爆炸（explosion）、命中（impact）与光束表现（beam），不含任何伤害/弹道数值（弹道数值在 `TurretDef`）。炮塔经 `art.projectile` 按条目 id 引用；条目缺失或不可用 → 回退几何弹丸/默认光束表现。

- 条目类别（`ProjectileArtKind`）与炮塔类型存在映射约定：`direct→bullet（实弹）`、`lob→shell（抛射）`、`missile→missile（导弹）`、`beam→ray（射线）`；类别不匹配仅黄警提示，允许跨类借用、按配置生效。
- 光束表现组（`beam`）挂在弹丸库 **ray 条目**上（仅 `kind:'ray'` 有意义）：射线类炮塔（持续光束与脉冲点射均可）引用 ray 条目即同时获得光束分层贴图表现与命中特效；旧配置中炮塔 `art.beam` 字段读取忽略，由引用条目的 beam 组接管。解析链：`def.art?.projectile → 弹丸条目 → entry.beam`（art.ts `beamArtConfig`）。
- ray 条目不提供尾焰与爆炸编辑（hitscan 无弹道飞行）；未配置尾焰的弹丸**无任何默认尾迹**（实弹无默认曳光线，导弹喷口焰随尾焰配置门控）。
- 出厂条目：bullet_std 标准实弹（bullet）、shell_std 标准榴弹（shell）、rocket_std 标准火箭（missile）、ray_std 标准射线弹（ray，含光束表现组）、custom_ammo_1 集束导弹（missile，猎手塔挂载）、custom_ammo_2 炮弹（bullet，两门加农炮挂载）。

### 1.4 挂载/炮位/堡垒的关系（简述）

炮塔不独立存在于战场格子，而是挂载到堡垒的炮位（`Hardpoint`）上，或由场景直接放置（地面炮塔）。要点：

- `TurretDef.mount`（S/M/L）必须匹配炮位 `size`；型号同时决定占格（`MOUNT_FOOT`：S=1×1 / M=1×2 / L=2×2，占格长/宽随型号联动归一化）。
- 挂载校验（engine.ts `mountTurret` 前置检查）：炮位存在且非内置隐藏硬点、型号匹配、炮位 `types` 白名单（未配置=不限类型）包含该炮塔类型、炮位未被占用、金币足够。
- 炮位的射界（`arc` 指定视角 / `fixed` 固定视角）、隐藏内置炮位（`hidden`+`builtIn` 预挂不可卸载）、渲染层级（`zLevel`）等属堡垒域，详见《堡垒编辑器设计》，本文不展开。

---

## 2. 参数全集

### 2.1 TurretDef 逐参数表

类型与默认值以 `config.ts` `interface TurretDef` 及引擎（engine.ts）/校验（art.ts `validateArt`）实际读取逻辑为准。单位约定：射程/爆炸半径用米（m），`M_PER_CELL = 25`（1 格 = 25 m）；占格/美术坐标用格；角度用度；速率用 每秒。

| 参数 | 中文名 | 类型 | 必填 | 默认值/缺省 | 取值范围/约束 | 现行语义 |
|---|---|---|---|---|---|---|
| id | 标识 | string | 是 | — | 唯一 | 炮塔定义 id；素材文件夹解析链兜底键 |
| name | 名称 | string | 是 | — | — | 显示名 |
| type | 炮塔类型 | WeaponType | 是 | — | direct/lob/missile/beam/spray | 决定弹道模型与可用参数子集（见 §1.2） |
| rayMode | 射线子模式 | 'pulse'\|'beam' | 否 | 'beam' | 仅 type:'beam' 有意义 | 'beam'=宽幅持续光束（发射期转向速度 ×0.5 削减）；'pulse'=点射单体即中、不穿透、正常转向 |
| desc | 描述 | string | 是 | — | — | 说明文案（纯展示，不参与逻辑） |
| cost | 造价 | number | 是 | — | — | 建造成本（金币） |
| rotateSpeed | 旋转速度 | number | 是 | — | 度/秒 | 炮塔跟踪转向速度；0 = 固定朝向（持续光束发射期间再 ×50% 削减） |
| aimCone | 射角 | number | 是 | — | 度 | 以炮口方向为中心的总角度；目标进入 ±aimCone/2 内免转炮直接瞄准 |
| rangeMin | 最小射程 | number | 是 | — | m | 近于此距离的目标不索敌 |
| rangeMax | 最大射程 | number | 是 | — | m | 超出不索敌；弹丸射程消亡口径同此 |
| canAir | 对空 | boolean | 是 | — | — | 可攻击空中单位 |
| canGround | 对地 | boolean | 是 | — | — | 可攻击地面单位 |
| damage | 伤害值 | number | 是 | — | — | 单发/单次伤害；喷射类不使用（伤害由 dot 表达），光束模式取 0 |
| projectileSpeed | 弹丸速度 | number | 否 | 直射 200 / 抛射 60 | m/s | 直射/抛射实弹飞行速度；导弹不使用（走导弹专用参数） |
| guided | 制导开关 | boolean | 否 | false | 仅 missile | false=常规（直飞锁定落点）；true=制导追踪（前置量）；true+guideDelay>0=延迟制导 |
| guideDelay | 延迟制导时间 | number | 否 | 未配置=立即制导 | 0.05~2 s（引擎钳制 0~2） | 发射后沿炮塔方向直飞该秒数再开启制导追踪 |
| guideDecel | 延迟减速度 | number | 否 | 0=延迟期照常加速 | ≥0，m/s² | 仅延迟期内生效，速度减到 0 为止 |
| missileFlightTime | 飞行时间上限 | number | 否 | 不限 | 秒 | 耗尽未命中则逐渐消失 |
| missileCurve | 飞行曲线系数 | number | 否 | 0=直线 | 0~100 | 蛇形往复摆幅（不占转向机构角速度） |
| ejectAngle | 出膛偏角 | number | 否 | 0 | 度 | 延迟制导期内初始航向=炮塔方向+偏角（侧抛/垂发）；仅 guideDelay>0 有意义 |
| burnTime | 燃烧时间 | number | 否 | 全程燃烧 | 秒 | 期内正常加速，燃尽后惯性滑行；渲染侧同步熄灭尾焰/喷口焰 |
| split | 真集束分裂 | object | 否 | 不分裂 | 见下行子字段 | 触发后母弹裂为 count 颗扇形子弹：伤害均分、不再分裂、继承制导/锁定/剩余飞行时间 |
| split.count | 集束子弹数 | number | 组内必填 | — | ≥2（<2 视同不分裂） | 子弹颗数 |
| split.spread | 集束扇角 | number | 组内必填 | — | 度 | 子弹以母弹航向为中心 ±半角对称展开 |
| split.at | 分裂时机 | 'proximity'\|'burnout' | 组内必填 | — | — | proximity=距目标进入触发距离时分裂；burnout=燃烧时间耗尽时分裂 |
| split.range | 分裂距离 | number | 否 | 25 | m | 仅 at:'proximity' 有意义 |
| missileInitSpeed | 导弹初速度 | number | 否 | 0=从静止加速 | ≥0，m/s | 发射瞬间速度；可大于极速，超出后不再加速仅维持 |
| missileAccel | 导弹加速度 | number | 否 | 40（引擎兜底） | m/s² | 燃烧期内速度爬升率 |
| missileMaxSpeed | 导弹极速 | number | 否 | 100（引擎兜底） | m/s | 仅低于上限才加速（不向下钳制初速） |
| missileTurnMax | 最大转向 | number | 否 | 120（引擎兜底） | 度/秒 | 制导转向角速度上限（脱靶不丢锁，盘旋回绕继续追） |
| missileTurnAccel | 转向加速度 | number | 否 | 240（引擎兜底） | 度/秒² | 转向角加速度 |
| accuracy | 精度 | number | 否 | 0=无散布 | m | 命中偏差半径（非制导导弹/直射/抛射生效；制导弹为 0 散布） |
| pierce | 穿透 | object | 否 | 不穿透（count 0） | 见子字段 | 弹丸最多作用 1+count 个目标，每穿透一个伤害 ×(1−decay) |
| pierce.count | 穿透数量 | number | 组内必填 | — | ≥0 | 可穿透的敌人数量 |
| pierce.decay | 穿透衰减 | number | 组内必填 | — | 0~1 | 每穿透一个目标的伤害衰减比例 |
| blastRadius | 爆炸半径 | number | 否 | 直射：未配置不爆炸；抛射落点缺省 15；导弹缺省 20（近炸引信判定缺省 18） | m | >0 时命中/落点爆炸波及半径内所有敌人；直射直击伤害与爆炸伤害分离 |
| blastEffect | 爆炸效果 | object | 否 | — | 见子字段 | 爆炸附加伤害与持续燃烧 |
| blastEffect.damage | 爆炸附加伤害 | number | 组内必填 | — | — | 叠加在直接命中上的爆炸伤害 |
| blastEffect.burn | 爆炸燃烧 | object | 否 | 无燃烧 | — | 持续伤害组 |
| blastEffect.burn.damage | 燃烧每跳伤害 | number | 组内必填 | — | — | 每跳伤害值 |
| blastEffect.burn.interval | 燃烧间隔 | number | 组内必填 | — | 秒 | 伤害跳间隔 |
| blastEffect.burn.duration | 燃烧时长 | number | 组内必填 | — | 秒 | 燃烧总时长 |
| beamWidth | 射线宽幅 | number | 否 | 判定缺省 2 m；渲染未配置=贴图原生高度 | m | 光束判定宽度；已配置时同时驱动贴图渲染宽度（渲染高度=宽幅/25 格，贴图/几何外观一致）；仅 rayMode:'beam' 有意义 |
| sprayAngle | 喷射角度 | number | 否 | 60 | 度 | 喷射扇形总角度（仅 spray） |
| attackDuration | 攻击持续 | number | 否 | 1 | 秒 | 射线/喷射单次开火持续时间 |
| dot | 持续伤害 | object | 否 | — | 见子字段 | 持续型（光束/喷射）每跳伤害；引擎按 `dot!` 读取，持续型炮塔必须配置 |
| dot.damage | 每跳伤害 | number | 组内必填 | — | — | 持续伤害每跳数值 |
| dot.interval | 伤害间隔 | number | 组内必填 | — | 秒 | 持续伤害跳间隔 |
| fireRate | 射速 | number | 是 | — | s/轮 | 每轮射击的间隔秒数，越小射得越快；导弹复挂节奏 = fireRate/(burst+1) 秒/枚 |
| burst | 连发数 | number | 否 | 1 | ≥1 | 每轮连发弹数；导弹塔同时为挂载弹数（初始满挂 max(1,burst)） |
| burstInterval | 连发间隔 | number | 否 | 0 | 秒 | 连发之间的间隔 |
| barrels | 炮管数 | number | 否 | 1（单管） | ≥1 整数 | 逻辑炮管数；持续型光束/喷射不适用；渲染/出生点以 art.barrels 挂点表为准 |
| barrelMode | 发射模式 | 'salvo'\|'sequential' | 否 | salvo | 多管有意义 | 齐射=全部炮管同时；轮流=逐管按连发间隔依次发射 |
| art | 美术配置 | TurretArt | 否 | 未配置=整体几何回退 | 见 §2.2 | 美术资源配置组 |
| reload | 装填/冷却 | number | 否 | 持续结束冷却缺省 = fireRate 轮间隔；喷射弹药耗尽中断缺省 1 | 秒 | 射线/喷射攻击持续结束后的装填冷却 |
| heatPerShot | 热量/发 | number | 否 | 0 | ≥0 | 每发产热汇聚到堡垒热量池；堡垒过热则全炮塔停火，降至上限 ×50%（OVERHEAT_RESUME）恢复 |
| armorPen | 穿甲比例 | number | 否 | 0 | 0~1 | 对堡垒命中时该比例伤害绕过装甲直入结构；为敌对堡垒等未来目标预留 |
| armorDamage | 削甲/命中 | number | 否 | 按伤害与穿甲比例推导 | ≥0 | 穿甲命中后削弱受击面装甲的显式数值 |
| chargeTime | 充能时间 | number | 否 | 无前摇 | 秒 | 每次开火周期起射前的前摇（仅射线类提供编辑器入口）；充能末帧亮起后滞留 0.05s（CHARGE_LAST_HOLD）才发射，目标丢失则取消 |
| ammoPerShot | 弹药/发 | number | 否 | 0 | ≥0 | 每发消耗弹药量（连发按 发数×单发 一次性校验） |
| ammoPerSec | 弹药/秒 | number | 否 | 0 | ≥0 | 喷射持续期间每秒弹药消耗；耗尽即中断并进入装填 |
| energyPerShot | 发射电量 | number | 否 | 0 | ≥0 | 每发/每次起射消耗电量（脉冲点射按发计，光束起射扣一次） |
| energyPerSec | 维持电量 | number | 否 | 0 | ≥0 | 光束持续发射期间每秒耗电；耗尽即中断 |
| gpu | GPU 占用 | number | 是 | — | — | 占用 GPU 算力（建造配额） |
| mount | 炮位型号 | MountSize | 是 | — | S/M/L | 只能挂到匹配 size 的炮位；占格 w/h 随之联动（S=1×1 / M=1×2 / L=2×2，由 `applyMountFoot` 归一化） |
| w | 占格长 | number | 是 | 随 mount | 由型号映射 | 占格长（格）；不可单独编辑，随型号联动 |
| h | 占格宽 | number | 是 | 随 mount | 由型号映射 | 占格宽（格）；同上 |
| hp | 耐久 | number | 是 | — | — | 结构耐久，归零被摧毁 |
| onDestroyBlast | 毁坏爆炸 | BlastEffect & {radius} | 否 | 无 | — | 被摧毁时以自身为中心爆炸（damage + radius，可带 burn） |
| color | 颜色 | string | 是 | — | #RRGGBB | 几何回退绘制用色 |
| tags | 索敌标签 | TurretTag[] | 否 | 未配置=现状（近堡垒优先、空军 ×0.5、无资源门控） | prefer 软排序 / exclude 硬过滤 / resource 开火门控 | 标签式索敌系统，详见第 3 章 |

补充：升级体系——炮塔最高 3 级（`MAX_LEVEL`），升级造价 = `round(cost × 0.9 × level)`，伤害/持续伤害按 `1 + 0.35 × (level − 1)` 倍率缩放。

### 2.2 TurretArt（art 美术组）逐参数表

坐标系：相对 `anchor`（轴心），炮口朝上基准系中 x 向右为正、y 向上（沿炮口方向）为正，单位格；素材炮口统一朝上绘制。美术坐标空间固定 30px=1 格（与贴图设计基准一致）。校验由 art.ts `validateArt` 执行（errors 硬性错误 / warnings 提示）。

| 参数 | 中文名 | 类型 | 默认值/缺省 | 取值范围/约束 | 现行语义 |
|---|---|---|---|---|---|
| anchor | 旋转轴心 | [x, y] | [0.5, 0.5] | 相对炮塔占格的归一化坐标对 | 炮身旋转中心 |
| barrels | 炮管挂点表 | {mount, muzzle, recoil?}[] | 未配置=按逻辑炮管数自动生成（单管挂点 [0,0]→炮口 [0,0.35]） | 非空数组；mount/muzzle 须数值对 | mount=炮管根部挂点；muzzle=炮口点（弹丸出生点与火光定位点）；数量≠逻辑炮管数时渲染/出生点以挂点表为准（warning） |
| barrels[].recoil | 逐管后坐（遗留） | number | — | ≥0 | 遗留字段，仅旧配置回退读取（读取链 `art.recoil ?? barrels[i].recoil ?? 0.1`），UI 不再提供 |
| recoil | 统一后坐 | number | 0.1 | ≥0，格；0=无后坐动画 | 所有炮管共用的后坐行程，优先于遗留逐管值 |
| glow | 辉光 | {overheatOnly?} | overheatOnly 默认 true | — | 默认仅过热时显示辉光 |
| zBias | 绘制次序微调 | number | 0 | — | 同层绘制次序微调 |
| spriteSet | 素材文件夹覆盖（遗留） | string | — | — | 遗留字段（UI 已移除），现行代码仍读取：文件夹解析链 = 素材库引用 → spriteSet ?? id 文件夹 |
| baseAsset | 底座素材 | string | 未配置=文件夹 base.png 回退 | 素材库条目 id；'none'=不绘制该层；'geo'=强制几何色块 | 底座层贴图（逐层降级：缺失层几何补绘） |
| turretAsset | 炮身素材 | string | 未配置=文件夹 turret.png 回退 | 同上 | 炮身层贴图 |
| barrelAsset | 炮管素材 | string | 未配置=文件夹 barrel.png 回退 | 同上；'none'=无炮管层（发射井/能量塔造型） | 炮管层贴图 |
| flashAsset | 开火效果素材 | string | 未配置=文件夹 flash.png 回退 | 素材库条目 id；'none'=不播放开火效果 | 火光帧条贴图；火光表现硬编码（2 帧、逐帧缩放 1.4×→1×、每帧 0.05s，见 FLASH_* 常量），无配置项 |
| charge | 充能动画 | {offset, frames, asset?} | 未配置=文件夹 charge.png 回退；无 chargeTime 时不生效（warning） | frames 为 1~12 整数 | 帧条按帧数横向等分、从左到右顺序播一遍；offset 相对轴心（格）；帧时长 = chargeTime/frames；asset='none'=不播放；充能末帧在攻击持续期内定格常显 |
| projectile | 弹丸引用 | string | 未配置=几何弹丸/默认光束 | PROJECTILE_ARTS 条目 id | 引用弹丸美术库条目；射线类引用 ray 条目 = 光束分层表现 + 命中特效；喷射引用不生效（warning）；类别不匹配黄警但按配置生效 |
| rack | 导弹挂载显示 | {show?, dx?, dy?} | show 默认 true；dx 默认 0；dy 默认 0.12 | dx/dy 为格 | 仅导弹塔：挂载弹显示开关与挂载点相对炮管挂点偏移 |

### 2.3 ProjectileArtDef 逐参数表

纯表现层定义，注册于 `PROJECTILE_ARTS`。尾焰/爆炸/命中为轻量粒子系统参数（实时程序化生成，无需素材）；旧 length/width/rings 等字段已废弃，兼容读取时忽略。

**主表**

| 参数 | 中文名 | 类型 | 默认值/缺省 | 取值范围/约束 | 现行语义 |
|---|---|---|---|---|---|
| id | 条目 id | string | — | 唯一 | 炮塔 `art.projectile` 引用键 |
| name | 名称 | string | — | — | 选择器显示名 |
| kind | 弹丸类别 | ProjectileArtKind | — | bullet/shell/missile/ray（实弹/抛射/导弹/射线） | 决定类别默认色（PROJECTILE_KIND_COLOR）与可编辑效果组 |
| projectileAsset | 弹体贴图 | string | 未配置=文件夹/通用回退 | 素材库条目 id；'none'=显式无贴图 | 本体贴图，优先于 spriteSet/id 文件夹解析链；不可用 → 几何回退 |
| trail | 尾焰组 | object | 未配置=无任何尾迹 | 见子表 | 弹尾持续喷粒子；模板默认 < 用户显式参数 |
| explosion | 爆炸组 | object | 未配置=无爆炸特效（ray 不提供） | 见子表 | 火花+烟尘粒子爆发 + 多层冲击环，尺寸/速度按爆炸半径 |
| impact | 命中组 | object | 未配置=无命中特效 | 见子表 | 碎屑飞溅粒子 + 中心亮点一闪；实弹命中点、持续光束 DoT 端点、脉冲点射命中点产生 impact 事件（ttl 0.15s） |
| beam | 光束表现组 | object | 未配置=贴图默认搭配 | 仅 kind:'ray' 有意义 | 光束分层贴图表现（光晕+亮芯+双端闪光+三组粒子），见子表 |
| spriteSet | 素材文件夹覆盖（遗留） | string | — | — | 遗留字段（UI 已移除），现行代码仍读取：解析链 = 库引用 → spriteSet ?? id → 文件夹兜底 |

**trail 尾焰组子表**（组在=生效；ray 类不提供）

| 参数 | 中文名 | 类型 | 默认值/缺省 | 范围/约束 | 现行语义 |
|---|---|---|---|---|---|
| template | 行为模板 | 'standard'\|'inertia'\|'pulse'\|'smoke' | standard | — | 标准拖尾/惯性甩尾/火焰脉冲/烟雾弥漫 |
| color | 颜色 | string | 类别色（smoke 模板默认暗灰） | #RRGGBB | 粒子颜色 |
| colorEnd | 渐变末色 | string | 缺省=仅亮度渐隐不变色 | #RRGGBB | 寿命内 color→colorEnd 渐变 |
| rate | 速率 | number | 模板默认 | 粒/秒 | 发射速率 |
| life | 寿命 | number | 模板默认 | 秒 | 粒子寿命 |
| size | 尺寸 | number | 模板默认 | 格 | 粒子尺寸 |
| inherit | 惯性继承 | number | 模板默认 | 0~1 | 初速=弹速×inherit+反向余速（0=原地消散、1=完全随弹） |
| spread | 散开度 | number | 0.6 | 弧度 | 发射方向随机锥角 |
| grow | 尺寸变化率 | number | 0 | >0 膨胀 / <0 收缩 | grow>0 走 smoke32 非加法渲染（烟尘），其余加法发光 |
| fadeIn | 淡入时长 | number | 0 | 秒 | 粒子淡入 |
| smoke | 烟雾尾迹子组 | object | 组在=生效 | 见下行 | 与主尾焰并行的第二股粒子流（长存留灰烟，前 40% 寿命膨胀扩散、之后尺寸冻结渐隐） |
| smoke.rate | 烟尾速率 | number | 20 | 粒/秒 | 独立累积器发射 |
| smoke.life | 烟尾寿命 | number | 3 | 秒 | 粒子寿命（不受燃烧时间钳制） |
| smoke.color | 烟尾颜色 | string | #9A958E | #RRGGBB | 浅灰默认 |
| smoke.duration | 烟尾持续 | number | 整个燃烧期 | 秒；超过引用炮塔 burnTime 按 burnTime 钳制 | 点火后烟尾喷射窗口，结束即停喷（已有烟团自然消散） |

**explosion 爆炸组子表**（ray 类不提供）

| 参数 | 中文名 | 类型 | 默认值/缺省 | 范围/约束 | 现行语义 |
|---|---|---|---|---|---|
| color | 颜色 | string | 类别色 | #RRGGBB | 火花/烟尘颜色 |
| duration | 时长 | number | 默认 | 秒 | 矢量底闪时长 |
| sparks | 火花数 | number | 默认 | 粒 | 火花粒子数量 |
| smoke | 烟尘数 | number | 默认 | 粒 | 烟尘粒子数量 |
| speedJitter | 初速随机幅度 | number | 0.4 | 0~1 | 每粒速度 ×(1±jitter)，seeded 确定性 |
| lifeJitter | 寿命随机幅度 | number | 0.3 | 0~1 | 火花寿命随机 |
| turbulence | 湍流强度 | number | 0.6 | 0~2 | 烟尘漂移抖动幅度 |
| rings | 冲击环层数 | number | 1 | 1~4 | 多层冲击环 |
| ringSpeed | 环扩散速度系数 | number | 1 | — | 冲击环扩散速度倍率 |
| ringWidth | 环厚度 | number | 2 | px | 冲击环厚度 |
| bias | 方向偏置 | number | 0 | 0~1 | 0=火花向命中方向收束；1=完全锥形爆发 |
| inherit | 速度继承 | number | 0 | 0~1 | 火花/烟尘初速叠加弹速×inherit |
| fireball | 火球尺寸系数 | number | 1 | 0~2（0=关闭） | v2.54 径向渐变火球（白核→特效色→透明，加法混合，前 45% 时长快膨胀、幂律衰减） |
| shock | 软边环厚度系数 | number | 1 | 0~2 | v2.54 冲击环软边羽化（径向渐变环带）；0=旧细描边环 |
| flash | 瞬时照明强度 | number | 0.5 | 0~1（0=关闭） | v2.54 爆点大半径低透明光晕，0.15s 线性衰减（照亮战场的打光层） |
| streak | 火花拉丝 | number | 1 | 0/1 | v2.54 火花沿速度方向拉亮线（复用 v2.15 电焊拖尾画法）；0=圆点 |

**impact 命中组子表**

| 参数 | 中文名 | 类型 | 默认值/缺省 | 范围/约束 | 现行语义 |
|---|---|---|---|---|---|
| color | 颜色 | string | 类别色（PROJECTILE_KIND_COLOR） | #RRGGBB | 碎屑/亮点颜色 |
| duration | 时长 | number | 0.15 | 秒 | 特效时长（与 impact 事件 ttl 同口径） |
| spikes | 碎屑数 | number | 5 | 粒 | 飞溅碎屑数量 |

**beam 光束表现组子表**（仅 kind:'ray'；校验由 art.ts `validateBeamGroup` 执行：颜色/flicker/scrollSpeed/缩放/粒子组非法为 error，未知素材引用为 warning）

| 参数 | 中文名 | 类型 | 默认值/缺省 | 范围/约束 | 现行语义 |
|---|---|---|---|---|---|
| glowAsset | 光晕层素材 | string | builtin:beam/beam_glowA | 素材库条目 id（光束分类）；'none'=程序化纯色矩形 | 光晕层贴图，沿光束平铺滚动、加法发光 |
| coreAsset | 亮芯层素材 | string | builtin:beam/beam_coreA | 同上 | 亮芯层贴图（宽为光晕 ×0.5） |
| impactAsset | 命中点闪光 | string | /res/fx/glow16.png | 条目 id 或 /res/ 路径；'none'=不显示 | 光束端点/点射命中点闪光 |
| muzzleAsset | 炮口闪光 | string | /res/fx/glow16.png | 同上 | 发射点光球 |
| fringeColor | 光晕染色 | string | #78C8DC | #RRGGBB（error 校验） | 光晕层染色 |
| coreColor | 亮芯染色 | string | #F0FAFF | #RRGGBB（error 校验） | 亮芯层染色 |
| flicker | 闪烁幅度 | number | 0.15 | 0~1（error 校验）；0=不闪烁 | 亮度双正弦合成闪烁，相位按炮塔错开 |
| scrollSpeed | 滚动速度 | number | 96 | ≥0（error 校验）；0=静止 | 贴图沿光束方向滚动速度（美术 px/s，按 cell/30 缩放） |
| muzzleScale | 发射点闪光缩放 | number | 1 | >0（error 校验） | 炮口闪光尺寸倍率 |
| impactScale | 命中点闪光缩放 | number | 1 | >0（error 校验） | 命中闪光尺寸倍率 |
| absorb | 吸收粒子组 | {rate?, color?, size?} | 组在=生效：rate 12 粒/s、color=亮芯色、size 0.05 格 | rate ≥0、size >0、颜色 #RRGGBB（error 校验） | 发射点环带向心汇聚粒子 |
| scatter | 散发粒子组 | {rate?, color?, size?, angle?} | 组在=生效：rate 24 粒/s、color=光晕色、size 0.05 格、angle 360（全向） | 同上；angle 0~360（error 校验） | 命中点飞溅粒子（带电焊式速度拖尾亮线）；angle 为以朝射线源方向为 0° 的全锥角（90°=左右各 45°） |
| smoke | 烟尘组 | {rate?, color?, size?} | 组在=生效：rate 6 粒/s、color #3A3632、size 0.1 格 | 同上 | 命中点慢速烟尘（smoke32 非加法渲染） |

### 2.4 缺省/可选参数的行为约定（回退规则汇总）

1. **美术整体回退**：`art` 未配置 → 整体几何绘制；逐层降级——底座/炮身/炮管任一层贴图就绪即贴图渲染该层、缺失层几何补绘，三层全缺才整体几何回退；火光/辉光/充能为可选层，缺失不影响就绪判定。
2. **弹丸引用回退**：`art.projectile` 未配置 → 几何弹丸（曳光短线/几何圆点）与默认光束表现；引用条目不存在 → 同上回退（黄警）；类别不匹配 → 黄警但按配置生效；喷射类引用 → 不生效（黄警）。
3. **光束表现回退**：ray 条目无 beam 组或组缺省 → 贴图默认搭配（beam_glowA/beam_coreA + glow16 双端闪光 + flicker 0.15 + 滚动 96）；单层素材加载失败 → 该层回退等效程序化矩形；'none' → 显式程序化/不显示。
4. **尾焰空配置语义**：未配置 trail = 无任何默认尾迹（实弹无曳光线、导弹无喷口焰）；空配置即用户明确不要尾焰，引擎不擅自加戏。
5. **导弹可选参数链**：guided 缺省 false（常规）；guideDelay 缺省=立即制导；guideDecel 缺省/0=延迟期照常加速；missileInitSpeed 缺省 0；burnTime 缺省=全程燃烧；split 未配置或 count<2 = 不分裂（首次填入自动补默认 40°/近炸/25m）；missileCurve 缺省 0=直线。
6. **射速与连发**：burst 缺省 1、burstInterval 缺省 0、barrels 缺省 1、barrelMode 缺省 salvo；reload 缺省按 fireRate 轮间隔兜底（喷射弹药耗尽中断缺省 1s）。
7. **持续型硬依赖**：光束/喷射必须配置 dot（引擎非空读取）；beamWidth 判定缺省 2m、sprayAngle 缺省 60°、attackDuration 缺省 1s。
8. **资源消耗缺省 0**：heatPerShot/ammoPerShot/ammoPerSec/energyPerShot/energyPerSec 未配置即不消耗；热量汇聚堡垒池，过热迟滞解除阈值 = 上限 ×0.5。
9. **遗留字段兼容**：`art.spriteSet`、弹丸 `spriteSet`、`barrels[].recoil` 为遗留字段，UI 已移除但现行代码仍按解析链/回退链读取；旧 `/sprites/` 路径前缀经 `resCompatUrl` 自动重写为 `/res/`；旧配置中的 `art.beam`、人员（crew/minCrew）、制导律（guidance）、最大角度（arc）等字段读取忽略、无害保留。
10. **索敌缺省**：tags 未配置 = 现状索敌（近堡垒优先、空军 ×0.5、无资源门控），详见第 3 章。
## 3. 索敌系统（标签式索敌）

索敌系统的职责：每座炮塔每帧从战场的目标候选集中选出一个目标，并给出炮口期望角度与开火许可。现行实现位于 `src/game/engine.ts` 的 `targetCandidates()` / `tagScore()` / `aim()` / `resourceHold()`，配置类型位于 `src/game/config.ts`（`TurretTag` / `PreferTagKey` / `ExcludeTagKey` / `ResourceTagKey` / `TurretDef.tags`）。

### 3.1 目标候选集 targetCandidates

`targetCandidates(s)` 是全部索敌逻辑的候选注入点，当前实现返回 `s.enemies`（全部存活敌人，`aim()` 遍历时会跳过 `hp <= 0` 的个体）。

候选集被刻意抽成独立函数，作为**实体合流的预留钩子**：未来敌方堡垒、僚机、可拦截导弹、堡垒生产单位等实体并入战场时，只需把它们并入 `targetCandidates()` 的返回列表，即可整体被标签体系（prefer/exclude/resource）覆盖，索敌、评分、门控逻辑无需改动。

### 3.2 标签模型总览

标签存于单座炮塔定义 `TurretDef.tags?: TurretTag[]`，判别联合三类：

```ts
type TurretTag =
  | { kind: 'prefer'; key: PreferTagKey }                          // 软排序
  | { kind: 'exclude'; key: ExcludeTagKey }                        // 硬过滤
  | { kind: 'resource'; res: ResourceTagKey; op: 'lt' | 'gt'; value: number } // 资源门控
```

三类的语义分工：

| 类别 | 作用层 | 语义 |
| --- | --- | --- |
| prefer 软优先 | 评分（`tagScore`） | 各键贡献一个权重因子**连乘**到基准分，分数越小越优先；prefer 之间非条件、不互斥 |
| exclude 硬过滤 | 候选过滤（`aim`） | 命中的目标直接从候选集中剔除，不参与评分 |
| resource 资源门控 | 开火开关（`resourceHold`） | 条件成立即**禁止开火**（硬切换，见 §3.5） |

组合规则：所有 exclude 过滤与 resource 门控必须**全部通过（AND）**才允许开火；prefer 标签只影响"先打谁"，不影响"打不打"。

### 3.3 评分公式（tagScore）

`aim()` 对每个通过过滤的候选调用 `tagScore(s, c, e, def)`（`c` 为炮塔中心），取**分数最小**者为目标（`score < bestScore` 即替换）。

**基准分**：

```
基准分 = 含 nearTurret 偏好标签 ? 目标距炮塔中心 hypot(e.x - c.x, e.y - c.y)
                                 : 目标距堡垒中心 distToFortress(s, e)
```

即默认基准是"近堡垒优先"；配置 `nearTurret` 偏好后改为"近炮塔优先"。`nearFortress` / `nearTurret` 两个偏好不再产生额外因子，只体现在基准分的选择上。

**空中单位系数（兼容默认）**：当 prefer 标签中**没有** `air` 或 `ground` 键时，空中单位基准分 ×0.5（即优先拦截空军）；一旦配置了 `air` 或 `ground` 偏好，该默认系数不再叠加，由偏好因子接管。

**各 prefer 键的权重因子**（满足条件才乘，连乘到基准分）：

| 键 | 因子公式（代码表达式） | 效果 |
| --- | --- | --- |
| `air` | 空中单位：`score *= 0.5` | 空中优先 |
| `ground` | 地面单位：`score *= 0.5` | 地面优先 |
| `hpMax` | `score *= 1 / (1 + e.hp / 100)` | 血最多优先（当前血越高因子越小） |
| `hpMin` | `score *= 1 + e.hp / 100` | 血最少优先（当前血越高因子越大） |
| `sizeBig` | `score *= 1 / (1 + ed.size / 0.35)` | 大体型优先（体型 `size` 越大因子越小） |
| `sizeSmall` | `score *= 1 + ed.size / 0.35` | 小体型优先（体型越大因子越大） |
| `nearFortress` | 无因子，基准分 = `distToFortress(s, e)` | 近堡垒优先（即默认基准） |
| `nearTurret` | 无因子，基准分改为距炮塔 | 近炮塔优先 |

**候选过滤（aim 内的硬条件，先于评分）**：

1. `hp <= 0` 跳过；
2. 空/地适配：目标为空中且 `def.canAir === false` 跳过；目标为地面且 `def.canGround === false` 跳过；
3. exclude 标签：目标为空中且存在 `exclude:air` 跳过；目标为地面且存在 `exclude:ground` 跳过（预留键不过滤任何现行实体）；
4. 射程区间：目标距炮塔 `distM = hypot(dx, dy) * M_PER_CELL`（米，M_PER_CELL = 25），`distM < rangeMin` 或 `distM > rangeMax * (1 + turretRangeBonus(s))` 跳过——射程上限含火控雷达模块增益（增益池按炮塔数摊薄）。

### 3.4 exclude 硬过滤键

`ExcludeTagKey = 'air' | 'ground' | 'wingman' | 'missile' | 'fortress' | 'spawned'`。现行生效键为 `air`（不打空中）、`ground`（不打地面）；其余为预留键（见 §3.7）。过滤在 `aim()` 候选循环中执行，命中即 `continue`，被剔除目标不参与评分排序。

### 3.5 resource 资源门控（resourceHold）

资源门控是**开火硬开关**，在 `updateTurretBody()` 中紧邻堡垒过热检查统一拦截。

**百分比口径**（阈值 `value` 均为占上限的百分比 0–100）：

| 资源键 | 当前百分比（代码表达式） | 上限口径 |
| --- | --- | --- |
| `ammo` 弹药 | `ammoCap > 0 ? s.ammo / caps.ammoCap * 100 : 100` | `resourceCaps(s).ammoCap` = 基础 cap + 模块加成 |
| `energy` 电量 | `energyCap > 0 ? s.energy / caps.energyCap * 100 : 100` | `resourceCaps(s).energyCap` = 基础 cap + 模块加成 |
| `heat` 热量 | `heatCap > 0 ? s.fortress.heat / heatCap * 100 : 0` | 堡垒热量池上限 `fortressDef(s).heatCap` |
| `defense` 防御 | `maxHp > 0 ? s.fortress.hp / s.fortress.maxHp * 100 : 100` | 堡垒耐久上限 |

**判定语义**：遍历全部 resource 标签，`op === 'lt'` 时 `v < value` 即成立，`op === 'gt'` 时 `v > value` 即成立；**任一**标签条件成立 → `resourceHold()` 返回 true → 禁止开火（例如"弹药低于 30% 停火"配 `{ res:'ammo', op:'lt', value:30 }`，"热量高于 80% 停火"配 `{ res:'heat', op:'gt', value:80 }`）。未配置任何 resource 标签时恒返回 false（无门控）。

**触发即停火的硬切换**：门控成立当 tick 直接执行 `t.firing = false; t.burstLeft = 0; t.chargeLeft = 0; return`——持续型武器（光束/喷射）立即中断、连发中止、充能取消，与堡垒过热同一条路径；条件恢复成立后按正常流程重新索敌/充能起射。

### 3.6 零标签默认行为

`tags` 未配置（undefined/空数组）时的行为：

- 无 exclude 过滤 → 空/地适配仅由 `canAir` / `canGround` 决定；
- 无 prefer 因子 → 评分 = `distToFortress`（近堡垒优先），空中单位 ×0.5（兼容默认系数生效）；
- 无 resource 门控 → 永不因资源阈值停火（仅受堡垒过热与弹药/电量实际耗尽的约束）。

即：**零标签 = 近堡垒优先 + 空军 ×0.5 + 无资源门控**。

### 3.7 预留键位

`PreferTagKey` 与 `ExcludeTagKey` 的类型联合中已包含四个预留键：`fortress`（敌方堡垒）、`wingman`（僚机）、`missile`（可拦截导弹）、`spawned`（堡垒生产单位）。

现行行为：

- 评分：`tagScore()` 的 prefer 分支对这些键无对应 case，配置后为**无操作**（不产生因子）；
- 过滤：`aim()` 的 exclude 分支只检查 `air` / `ground`，预留键不过滤任何现行实体；
- 候选集：`targetCandidates()` 当前只含敌人列表，尚无这些类型的实体可被选中。

这些键是**类型位预留**：待对应实体上线并并入 `targetCandidates()` 后激活（届时补充分支逻辑），编辑器当前不向用户开放这些键的选取。

## 4. 开火行为与资源

本章描述炮塔从索敌到击发的完整行为链路，以及弹药/电量/热量三类资源的消耗与回复。全部语义以 `src/game/engine.ts` 的 `updateTurretBody()`（及 `aim()` / `fireGunShot()` / `addFortressHeat()` / `coolFortress()` / `tick()` 资源回复段）为准。

### 4.1 开火流程总览

`updateTurretBody(s, t, dt)` 每帧按固定顺序推进（效率系数 `factor` 恒为 1）：

1. **表现衰减**：导弹复挂动画计时 `rackAnim` 每 tick 无条件衰减；
2. **导弹复挂**（仅导弹塔）：一轮打空（`burstLeft === 0`）且未满挂时，每隔 `x = fireRate / (burst + 1)` 秒 `rackLeft + 1` 至满挂（`full = max(1, burst ?? 1)`）；轮中（含中断暂停）与满挂不复挂；复挂与索敌无关；
3. **堡垒过热检查**：`s.fortress.overheated` 为真 → `firing=false, burstLeft=0, chargeLeft=0` 并返回（全炮塔停火，解除由散热迟滞控制，见 §4.6）；
4. **资源门控检查**：`resourceHold()` 成立 → 同上硬切换停火（见 §3.5）；
5. **射线维持电量**：`type:'beam'` 炮塔（不限 rayMode，含脉冲点射）配置了 `energyPerSec` 时，每帧扣 `energyPerSec * dt`；电量不足则清零、`firing=false` 返回；
6. **冷却推进**：`cooldown -= dt`；
7. **索敌与转向**：调用 `aim()`（持续光束发射期间转速 ×0.5，见 §4.7）选目标、转炮、给出 `canFire`；
8. **充能前摇**（配置了 `chargeTime` 时，见 §4.3）；
9. **击发**：持续型（光束/喷射）走状态机（§4.4）；直射/抛射/导弹/脉冲点射走"轮 + 连发"流程（§4.2）。

整体节奏即：**索敌 → 转向对准 → （充能）→ 开火 → 冷却/装填**，任一环不满足即停在本环节等待或中止。

### 4.2 轮与连发（burst，实弹类）

适用类型：直射（direct）、抛射（lob）、导弹（missile）、脉冲点射（`type:'beam'` 且 `rayMode:'pulse'`）。

**术语**：`fireRate` 语义为 **s/轮**（每轮射击的间隔秒数，越小射得越快）；一轮含 `burst` 次击发，相邻击发间隔 `burstInterval` 秒。

**流程**：

- 空闲态（`burstLeft === 0`）：需目标存在、`canFire` 为真、`cooldown <= 0`，且弹药/电量预检通过（`s.ammo >= ammoPerShot`、`s.energy >= energyPerShot`，后者仅脉冲点射检查），才进入新一轮：`burstLeft = max(1, burst ?? 1)`，`burstTimer = 0`（当 tick 立即首发）；
- 轮内（`burstLeft > 0`）：`burstTimer -= dt`，计时未到则等待；到点后若目标丢失或 `canFire` 不成立（移出射程/射界），`burstLeft = 0` **中止本轮**；否则执行一次击发（§4.5 多管规则），扣资源 `ammo -= ammoPerShot * shots`、`energy -= energyPerShot * shots`（仅脉冲点射），产热 `addFortressHeat(s, heatPerShot * shots)`，导弹塔同时 `rackLeft -= shots`；随后 `burstLeft--`、`burstTimer = burstInterval ?? 0`；
- 击发后若本次产热使堡垒过热：中断连发（`burstLeft = 0, cooldown = 0`）；
- 本轮打完（`burstLeft === 0`）：`cooldown = fireRate`（s/轮语义），进入冷却等待下一轮。

资源不足（弹药/电量低于本次击发需求）时直接中止本轮（`burstLeft = 0`），恢复后重新走整轮流程。

### 4.3 充能前摇（chargeTime）

配置了 `chargeTime > 0` 的炮塔，每次开火周期起射前先充能 `chargeTime` 秒（实弹类每轮一次；持续型每次 attackDuration 周期一次；轮内连发/齐射/轮流**不重复充能**）。

状态机（`chargeLeft` 为剩余秒数）：

- 空闲且满足起射条件（目标存在、`canFire`、`cooldown <= 0`，持续型需 `!firing`、实弹类需 `burstLeft <= 0`）→ 进入充能：`chargeLeft = chargeTime`；
- 充能中（`chargeLeft > 0`）：每帧 `chargeLeft -= dt`；此期间**不射击、不产热、不耗弹药/电量**（视为"不射击"，堡垒可自然散热）；目标丢失或移出射程射界（`!target || !canFire`）→ 取消充能（`chargeLeft = 0`），重新索敌后需重新充能；
- **末帧滞留**：充能最后一帧不计入充能时间——`chargeLeft` 计到 0 后进入负值滞留段 `(-CHARGE_LAST_HOLD, 0)`（`CHARGE_LAST_HOLD = 0.05` 秒，精确落零以 `-1e-9` 兜底），滞留结束（`chargeLeft <= -0.05`）当 tick 起正常开火。

### 4.4 持续型武器（光束 / 喷射）

适用类型：喷射（spray）与持续光束（`type:'beam'` 且 `rayMode` 缺省或 `'beam'`）。脉冲点射不走本流程（走 §4.2 实弹流程）。

**状态机**：

- 起射（非 firing 态）：需目标存在、`canFire`、`cooldown <= 0`；预检资源——光束 `energyPerShot` 不足不起射，喷射配置了 `ammoPerSec` 且 `ammo <= 0` 不起射；通过则光束先扣 `energyPerShot`（发射电量），`firing = true`、`firingLeft = attackDuration ?? 1`、`tickTimer = 0`；
- 持续中（firing 态）：
  - 喷射按 `ammoPerSec * dt` 逐帧耗弹药，不足时清零停射并进入 `cooldown = reload ?? 1`；
  - DoT 结算：`tickTimer -= dt`，到点后按 `dot.interval` 周期结算一次伤害（光束 `beamTick` / 喷射 `sprayTick`），`tickTimer += dot.interval`；光束起射伸展期（长度未达全长/阻挡点）不消费计时器，伸展到位当帧立即结算首次伤害；
  - `firingLeft -= dt`，耗尽即本次持续结束：`firing = false`，`cooldown = max(reload ?? 0, fireRate)`——**装填** `reload` 与轮间隔 `fireRate` 取大者作为冷却。

持续光束起射后以 `BEAM_ON_SPEED = 2400 m/s` 从炮口伸展到全长（250m 射程约 0.10s）；停火（持续结束/目标丢失/资源中断/过热/门控）触发 0.25s（`BEAM_FADE`）收窄渐隐的消退段。

### 4.5 多联炮管（barrels / barrelMode）

`barrels` 为逻辑炮管数（默认 1 单管），多管时由 `barrelMode` 决定击发方式（缺省 `'salvo'`），持续型光束/喷射不适用：

- **齐射 salvo**：每次击发**全部炮管各射 1 枚**（`shots = nBarrels`），弹药/热量/电量均按实际弹丸数（×shots）结算；
- **轮流 sequential**：每次击发仅 1 枚，从 `barrelIdx` 炮管射出并轮转 `barrelIdx = (barrelIdx + 1) % nBarrels`；连发间隔即相邻两管的发射间隔，`barrelIdx` **跨轮不重置**。

弹丸出生点：导弹取挂载位（待发弹视觉位置，槽位对应该管当前枚数 -1，与展示消耗一致）；实弹/抛射取炮口点 `muzzlePos`。直射弹丸出生点沿弹道反向预偏一个 tick 行程（`traveled` 负初始化），首帧渲染恰好位于炮口。

### 4.6 资源消耗与回复

三类资源均为**堡垒级全局池**（非单炮塔独立），消耗口径见各开火流程，回复在主循环 `tick()` 中统一推进。

**弹药**：

- 池：初始 80，基础上限 160，基础回复 3 发/s（`AMMO = { start: 80, cap: 160, regen: 3 }`）；兵工厂等模块提供 `ammoCap` 上限与 `ammoRegen` 回复加成；
- 回复（每 tick）：`ammo = min(cap + mb.ammoCap, ammo + (3 + mb.ammoRegen) * dt)`；
- 消耗：实弹类按 `ammoPerShot * shots` 每击发；喷射按 `ammoPerSec` 每秒；耗尽/不足即停射。

**电量**：

- 池：初始 90，基础上限 150，基础回复 9 点/s（`ENERGY = { start: 90, cap: 150, regen: 9 }`）；模块提供 `energyCap`/`energyRegen` 加成；
- 回复（每 tick）：`energy = min(cap + mb.energyCap, energy + (9 + mb.energyRegen) * dt)`；
- 消耗：光束类按 `energyPerShot` 每次起射（脉冲点射按击发 ×shots），持续光束另按 `energyPerSec` 每秒维持（维持扣费在冷却推进与索敌之前执行，不足即中断）。

**热量（堡垒热量池）**：

- 产热：每次击发 `addFortressHeat(s, heatPerShot * shots)` 汇聚到堡垒热量池，封顶 `heatCap`；`heat >= heatCap` 即 `overheated = true`——**全炮塔停火**；
- 散热：`fortressCooling(s) = 堡垒自然散热 heatDissipation + 散热器模块冷却池`（散热器全额直连、不按炮塔数摊薄），每 tick `heat = max(0, heat - cooling * dt)`——射击中、过热中均持续散热；
- 过热解除迟滞：过热状态持续至 `heat <= heatCap * OVERHEAT_RESUME`（`OVERHEAT_RESUME = 0.5`）才解除，即攒满停火、降到半池复射；
- 出厂默认堡垒 `heatCap = 200`、`heatDissipation = 10` 点/s。

### 4.7 炮塔转向与跟随

**基角**：挂载炮塔的基角随船头朝向 `s.fortress.heading`，地面炮塔基角恒 0（朝上）。

**转速**：`rotateSpeed` 度/秒（`rot = rotateSpeed * factor * DEG`，`factor` 恒 1），每帧转向步长 `min(需转角度, rot * dt)`。持续光束（rayMode 为 beam）发射期间转速削减 50%（`BEAM_TURN_FACTOR = 0.5`），点射模式不受影响。

**射角（aimCone）**：以炮口方向为中心的总角度，免转瞄准锥半宽 `coneR = max(aimCone / 2, 4°)`（最小 4°）。目标在射角内时**无需转炮**直接视为对准；偏离射角时才旋转，且只转到目标进入射角即停（步长 `min(|diff| - coneR, rot * dt)`，炮口方向恒为射角中心）。开火条件 `aligned = |wrapAngle(目标方位 - 炮口角)| <= coneR`。

**视角约束**（挂载炮塔由炮位配置，三模式）：

- 固定视角：期望角恒为固定角，炮口不追踪目标；仅当目标自行进入射角锥时开火；
- 指定视角（视界区间）：目标须在视界区间内方可开火（`inArc`），期望角钳制进视界；
- 全视角：360° 自由旋转（炮塔级最大角度参数已取消）。

`canFire = inArc && aligned`——视界外或未对准不可射击。

**无目标回中**：无目标时炮口按转速缓慢转回休息角——固定视角回固定角，有视界回视界中点，否则回基角。

### 4.8 射击精度（accuracy 散布）

`accuracy` 为命中偏差半径（米）。散布取样（`accuracyOffset`）：

```
a = eventRandom(eventId, 0) * TAU
r = sqrt(eventRandom(eventId, 1)) * m2c(accuracy)  // 开平方保证圆盘内均匀
偏移 = (cos(a) * r, sin(a) * r)
```

即以瞄准点为圆心、`accuracy` 为半径的圆盘内**均匀随机**取命中点。随机样本由投射物事件 ID 与独立流编号派生，因此相同状态和输入必得相同结果，不依赖进程级 `Math.random`。各类型应用口径：

- 直射（direct）：瞄准点 = 目标位置 + 散布偏移，弹丸沿炮口→偏移点方向直线飞行；
- 抛射（lob）：落点 = 目标位置 + 散布偏移，抛体飞向该落点爆炸；
- 导弹（missile）：非制导发射瞬间锁定落点 = 目标位置 + 散布偏移（之后不再修正）；制导弹不加散布（追踪目标）；
- 脉冲点射 / 光束 / 喷射：不使用 `accuracy`（点射即时单体命中目标本体；持续型按光束宽幅/喷射扇形范围结算）。
## 5. 直射炮（direct）

直射炮发射实弹（bullet），弹丸沿直线匀速飞行，命中判定逐 tick 进行。弹道参数全部在 `TurretDef` 上配置，弹丸表现经 `art.projectile` 引用弹丸库条目（见第 7 章）。

### 5.1 实弹弹道

**弹速与飞行**

- `projectileSpeed`（m/s，缺省 200）：弹丸每 tick 前进 `弹速 × dt`，全程匀速，无加速/减速/下坠。
- 出生点：配置了 `art.barrels` 挂点表时取对应炮管的 `muzzle` 挂点（随炮塔当前角度换算到世界坐标）；未配置时单管取炮塔中心、多管沿垂直炮口方向均布并沿炮口前伸 0.35 格。
- 出生点沿弹道**反向预偏一个 tick 行程**（v·dt）：开火（tick 步骤 4）先于弹道推进（步骤 5），同 tick 内弹丸会被立刻前移 v·dt，预偏后首个可见帧恰好位于炮口，弹速再快也不会出现"出生即离口"的脱节；`traveled` 相应负初始化，射程消亡口径不变。

**精度散布**

- `accuracy`（m，命中偏差半径）：以目标位置为圆心、accuracy 为半径在圆盘内均匀随机取一个命中点（`sqrt(random)` 半径采样保证面密度均匀），弹丸朝该点飞行。accuracy 越大散布越开；0 = 指哪打哪。
- 散布只在开火瞬间决定弹道方向，飞行途中不再修正。

**射程**

- `rangeMin` / `rangeMax`（m）：索敌阶段即生效——目标距离在区间外不可瞄准（近距死区 + 远距上限）。`rangeMax` 受火控雷达射程增益影响（增益池按炮塔数摊薄）。
- 弹丸自身射程 `maxTravel = rangeMax + 25m`（一格余量，防止恰好贴边目标脱漏）；`traveled ≥ maxTravel` 或飞出战场边界即消亡，不产生任何效果。

**穿透 pierce**

- `pierce: { count, decay }`：一发弹最多作用 `1 + count` 个目标（count=0 = 命中第一个目标即消失）。
- 命中判定为**线段-圆形**：每 tick 以上一帧位置→当前位置的线段对每个存活敌人做最近点判定，命中半径 = 敌人体型 + 0.06 格；空军目标在炮塔 `canAir=false` 时不参与判定。
- 同一 tick 命中的多个目标按沿程先后排序结算；已命中的敌人记录在 `hitIds` 中不重复命中。第 1 个目标吃 100% 伤害，之后每穿透一个目标伤害 ×`(1 − decay)^已命中数`；命中数达到上限弹丸消失。

**命中与命中事件**

- 命中敌人：结算伤害（含等级倍率与穿透衰减）；若炮塔配置了 `art.projectile`，在敌人身体视觉中心（脚底锚点上方 size×身高系数处，加每次不同的确定性抖动）推一条 impact 事件（ttl 0.15s），由弹丸库条目的命中参数驱动碎屑特效；未配置弹丸条目则不产生事件、无任何命中特效。
- 命中阻挡弹道的物体：弹丸消失不穿透，物体有耐久则扣耐久（归零摧毁）。
- 配置了爆炸（`blastRadius > 0`）的直射弹在命中敌人或物体时同时触发爆炸：直击目标吃直击+爆炸，波及目标只吃爆炸（爆炸伤害基底 0，附加/燃烧由 `blastEffect` 提供），遮挡豁免与波及口径与榴弹爆炸一致。

### 5.2 点射/连发行为在直射上的表现

直射走"**轮 + 连发**"模型（`fireRate` / `burst` / `burstInterval` / `barrels` / `barrelMode`）：

- `fireRate`（s/轮）：每轮射击的间隔秒数，越小射得越快。一轮打完后进入该时长的冷却。
- `burst`（连发数，缺省 1）+ `burstInterval`（s）：一轮内连续击发 burst 次，相邻击发间隔 burstInterval。
- 多管 `barrels` + `barrelMode`：
  - `salvo` 齐射（多管缺省）：每次击发全部炮管各射 1 枚，弹药/热量/电量按实际弹丸数结算；
  - `sequential` 轮流：每次击发仅当前管射 1 枚，之后轮转下一管——连发间隔即相邻两管的发射间隔，炮管序号跨轮不重置。
- 中断规则：轮中目标丢失/移出射程射界、资源（弹药）不足、堡垒过热，均立即中止本轮剩余连发；过热中断额外清零冷却。
- 配置了充能时间 `chargeTime` 时，每轮起射前先充能（充能期间不射击、不涨热、不耗资源）；目标在充能期间丢失则取消充能、重新索敌后重新充能。轮内连发/齐射不重复充能。
- 每发产热 `heatPerShot × 实际弹丸数`，汇聚到堡垒热量池；每发耗弹 `ammoPerShot × 实际弹丸数`。

### 5.3 弹丸视觉（概述）

- **弹体**：经 `art.projectile` 引用弹丸库 bullet 条目；有条目且贴图就绪时按**原图尺寸**绘制（1 贴图像素 = 1 画布像素 × 缩放，宽高比保持），随飞行航向旋转。无条目/无贴图/`'none'` 显式无贴图时回退几何弹丸（米黄描边圆点）。
- **尾焰/曳光**：尾部表现完全由弹丸条目 `trail` 尾焰组驱动（程序化粒子，模板+参数，见第 7 章）；**不配置尾焰即飞行时只有弹体、无任何默认尾迹**——引擎不提供任何程序化曳光回退，也不在开火瞬间向目标拉发射直线。
- 贴图、尾焰模板、颜色渐变等美术细节见第 11 章。

## 6. 导弹（missile）

导弹发射后自驱动飞行：有速度、转向速率等状态，逐 tick 积分推进。与直射/抛射最大的不同是**制导与发动机模型**——导弹可以追踪目标、有燃烧时间、有飞行寿命。

### 6.1 制导模式与锁定行为

`guided` 与 `guideDelay` 组合出三种模式：

| 模式 | 配置 | 行为 |
|---|---|---|
| 常规（非制导） | `guided: false` | 发射瞬间按精度散布锁定**落点坐标**，之后不再修正；飞行中撞上任何敌人即在其处爆炸（沿途撞击）；到达锁定点即爆炸（目标移动不改变落点） |
| 制导 | `guided: true` | 出生即锁定目标（真锁定 `targetId`），全程前置量追踪 |
| 延迟制导 | `guided: true` + `guideDelay > 0`（0.05~2s） | 发射航向 = 炮塔方向 + `ejectAngle` 出膛偏角（度，缺省 0；可做侧抛/垂发），延迟期内沿该航向直飞——不追踪、不触发锁定点爆炸；延迟归零自动开启制导追踪 |

**真锁定与重锁**：制导弹出生时记录目标 `targetId`，此后只认该目标。**仅当目标死亡/不存在时才重锁**（`retargetMissile`）：候选按空/地规则过滤后，优先取"剩余飞行时间内可达"者（距离 ÷ 极速 ≤ 剩余飞行时间 × 0.9 机动余量）中最近者；无可达候选则取全体最近者（允许追不上，之后由飞行寿命收尾）。重锁时近炸引信基线与前置量采样基线同步重置。完全无候选时保持直飞最后已知位置。

**脱靶不丢锁**：制导导弹全程保持追踪——即使越过目标、转向能力不足也不失去锁定，每 tick 以转向能力上限持续向目标方向收敛，转向不足就盘旋回绕继续追。收尾由三条路径承担：

1. **近炸引信**：与目标距离由减转增（通过最近点）且仍在近炸半径内（`blastRadius` 折算 + 0.6 格）时擦爆；直接命中（距离 ≤ 敌人体型 + 0.45 格）同样爆炸。
2. **飞行时间** `missileFlightTime`（秒，未配置 = 不限）：耗尽未命中则进入 0.5s 淡出——停止制导、惯性直飞、不再命中/爆炸，淡出结束移除。
3. **射程终点**：`traveled ≥ rangeMax × 1.3` 就地爆炸；飞出边界直接消失。

**前置量制导**：制导恒为前置量（lead）追踪——按目标速度预估拦截点。敌人无速度字段，由导弹侧按相邻 tick 目标位移 ÷ dt 采样估算；预估命中时间 = 距离 ÷ 当前弹速（封顶 2s 防远距过冲），迭代 1 次。首帧无采样基线时退化为直飞目标当前位置。转向机构：`turnRate` 从 0 按 `missileTurnAccel`（度/s²）爬升，上限 `missileTurnMax`（度/s）。

**飞行曲线**：`missileCurve`（0~100，缺省 0 = 直线）在基础航向（制导追踪/非制导锁定航向）上叠加余弦航向偏置——偏置 = (curve/100) × 90° × cos(2π × 0.8Hz × 弹龄 + 相位），相位每发随机，往复摆动非单边扭转；weave 为气动摆动，不占用转向机构角速度，也不改变命中/过期判定（均按弹体实际位置结算）；贴图与喷口跟随同一可视航向。

**阻挡物体**：导弹飞行途中不与阻挡弹道的物体碰撞（一般可越过）；仅当发射瞬间"炮口→目标"线段穿过阻挡物体、且目标格紧邻物体后方（沿线格距 ≤ 物体高度 `height`）时，导弹退化为非制导直飞物体位置爆炸并结算物体耐久。

### 6.2 发动机与速度模型

- **初速度** `missileInitSpeed`（m/s，缺省 0 = 从静止加速）：发射瞬间的标量速度；可大于极速，超出后不再加速仅维持。
- **加速**：燃烧期内按 `missileAccel`（m/s²）加速，上限 `missileMaxSpeed`（缺省 100）。
- **延迟减速** `guideDecel`（m/s²，缺省 0 = 延迟期照常加速）：仅延迟制导期内生效，速度减到 0 为止（悬停式垂发）。
- **燃烧时间** `burnTime`（秒，未配置 = 全程燃烧）：期内正常加速；燃尽后惯性滑行——不再加速，渲染侧同步熄灭主尾焰/烟尾/喷口焰。
- **点火时刻**：弹龄计时基准 `igniteAtT` = 出生时的制导延迟秒数（无延迟 = 0 = 出生即点火）。点火后 1s 内主尾焰为**大力喷射**（发射速率 ×3→×1、尺寸 ×1.6→×1 线性回落），之后转常规尾焰；大力喷射是发动机点火行为，**仅导弹生效**，实弹/抛射从出生即常态尾焰。

### 6.3 发射架/挂架（rack）

导弹塔有"待发弹挂载"表现与装填节奏（`art.rack` + 引擎 rackLeft 状态）：

- `art.rack`：`show`（默认 true）控制挂载显示开关；`dx`/`dy` 为挂载点相对炮管挂点的偏移（格，默认 0 / 0.12）。
- 待发弹余量 `rackLeft` 初始满挂 = `burst`；逐发 −1、齐射 −N；多管时余量均分到各管展示（轮流模式当前管优先消耗先空）。
- **逐枚复挂**：一轮打空后启动计时，每隔 `fireRate / (burst + 1)` 秒复挂 1 枚直至满挂；轮中（含中断暂停）与满挂不复挂，复挂与索敌无关。每枚复挂播 0.25s 渐显推入动画（仅最新一枚）。
- 导弹**出生点 = 挂载位**（待发弹的视觉位置），而非炮口点；出生即按当前航向与初速起飞。

### 6.4 烟尾与喷口焰（概述）

- **烟尾**（`trail.smoke` 子组，组在=生效）：与主尾焰并行的第二股粒子流——烟团尺寸为主尾焰 1.6 倍，初速 = 弹速 ×0.15 少量惯性 + 横向弥散，前 40% 寿命膨胀扩散、之后尺寸冻结并渐隐至消失，非加法渲染。参数：速率（缺省 20 粒/s）、寿命（秒，缺省 3；不受燃烧时间限制——烟可比发动机工作更久地存留）、颜色（缺省浅灰 #9A958E）、**持续**（秒，可选）：点火后的烟尾喷射窗口，结束即停喷（已有烟团自然消散）；窗口超过引用炮塔燃烧时间时按燃烧时间钳制，缺省 = 整个燃烧期。
- **喷口焰**：导弹尾部引擎喷口高亮（加法混合、亮度闪烁），随尾焰配置门控——配置了尾焰才画；未点火（延迟期）/燃尽滑行期不画。
- **发动机门控**：未点火（制导延迟期）与燃尽滑行期，主尾焰、烟尾、喷口焰全部熄灭；点火/燃尽切换即时生效。
- 美术细节见第 11 章。

### 6.5 载体速度继承

堡垒挂载炮塔（有 `hardpointId`）发射的导弹，**出生速度向量 += 堡垒移动速度**：出生瞬间一次性合成（堡垒速度 格/s 换算 m/s 后向量相加），合成结果折回"航向 + 标量初速"（合成恰好抵消归零时保持原航向）。移动中开火的导弹因此与炮口/挂架不脱节。点火制导后导弹完全自驱动，与常规一致；地面炮塔不继承。

### 6.6 集束分裂（split）

`split: { count, spread, at, range? }` 真集束分裂：

- 触发：`at: 'proximity'` = 制导弹距目标 ≤ `range` 米（缺省 25）；`at: 'burnout'` = 燃烧时间耗尽。
- 母弹裂为 `count` 颗扇形子弹（±spread/2 对称展开），伤害均分，继承制导状态/锁定目标/剩余飞行时间/弹龄；weave 相位随扇角错开，避免子弹叠摆成一束。
- 子弹带 `splitDone` 标记不再二次分裂；子弹出生即点火（点火时刻 = 分裂时刻弹龄，大力喷射/烟尾持续窗口按分裂时刻起算）。
- 分裂瞬间在母弹位置产生一次小型碎裂闪光（impact 事件），母弹随即移除。

### 6.7 与直射的关键差异

| 维度 | 直射（bullet） | 导弹（missile） |
|---|---|---|
| 弹道 | 直线匀速，方向在开火瞬间确定 | 自驱动：初速/加速/极速/延迟减速/燃尽滑行 |
| 命中方式 | 线段扫描 + 穿透多目标，单体伤害逐穿衰减 | 单点爆炸（blastRadius 范围伤害），无穿透 |
| 目标关系 | 无锁定概念，散布决定弹着 | 真锁定 targetId，目标死亡才重锁，脱靶不丢锁 |
| 精度散布 | 每发按 accuracy 散布 | 制导不受影响；非制导仅影响锁定落点 |
| 收尾 | 射程（rangeMax+25m）/出界即消亡，无爆炸 | 射程终点（rangeMax×1.3）就地爆炸；飞行时间耗尽淡出 |
| 阻挡物体 | 撞上即消失（扣耐久/可触发爆炸） | 一般越过；仅发射时目标紧邻物体后方才被挡 |
| 出生点 | 炮口挂点（反向预偏一个 tick 行程） | 挂载位（待发弹位置），继承堡垒移动速度 |
| 发射节奏 | 轮+连发即全部 | 额外有挂架余量/逐枚复挂表现 |
| 尾部表现 | 尾焰粒子（配置驱动，无默认尾迹） | 尾焰 + 烟尾 + 喷口焰 + 点火大力喷射，受发动机门控 |

## 7. 弹丸系统与弹丸库

### 7.1 弹丸库的结构与定位

弹丸库（`PROJECTILE_ARTS: ProjectileArtDef[]`）是全部炮塔共享的**弹丸美术/特效统一注册表**。炮塔经 `art.projectile` 引用条目 id；条目不存在/无素材时回退几何弹丸（射线类回退默认光束搭配），不影响战斗逻辑。

**条目结构**（`ProjectileArtDef`）：

| 字段 | 说明 |
|---|---|
| `id` / `name` | 条目 id / 显示名 |
| `kind` | 弹丸类别：`bullet` 实弹 / `shell` 炮弹（抛射）/ `missile` 导弹 / `ray` 射线；决定默认特效色与适配炮塔类型（直射→bullet、抛射→shell、导弹→missile、射线→ray；允许跨类借用，编辑器给出类别不匹配提示，按配置生效） |
| `projectileAsset` | 弹丸本体贴图：素材库「弹丸」分类条目 id；`'none'` = 显式无贴图（几何回退）；缺省 = 按条目 id 文件夹 `/res/projectiles/{id}/projectile.png` 回退，缺失即几何弹丸 |
| `trail` | 尾焰组（组在=生效）：行为模板 + 粒子参数 + 可选烟尾子组，见 §7.4 |
| `explosion` | 爆炸组（组在=生效）：火花+烟尘粒子爆发 + 多层冲击环 |
| `impact` | 命中组（组在=生效）：碎屑飞溅 + 中心亮点一闪 |
| `beam` | 光束表现组（仅 `ray` 条目有意义）：光晕/亮芯分层贴图 + 闪光 + 三组粒子，见 §7.5 |
| `spriteSet` | 遗留字段（编辑器不再提供）：素材文件夹名覆盖，兼容读取 |

特效（尾焰/爆炸/命中）一律由**程序化粒子参数**实时生成，无需序列帧素材；本体贴图为单张静态图。

**注册表约定**：

- `PROJECTILE_ARTS` 为**可变注册表**：编辑器就地修改条目字段/增删条目并 bump 即时生效，引擎与渲染零改动。
- **持久化**：每次编辑经 bump 将全量 JSON 写入 localStorage（`td-projectile-arts`，带版本信封）；应用启动时加载并就地替换注册表内容，解析失败/无存储环境静默使用出厂默认。「恢复出厂」清回出厂快照并删除存储键。
- **口令**：导出/导入口令携带弹丸库全量（`projectileArts`），导入以口令数据为准整体替换。
- 内置条目（`bullet_std` 标准实弹 / `shell_std` 标准榴弹 / `rocket_std` 标准火箭 / `ray_std` 标准射线弹）在编辑器内**不可删除**；新建条目 id 自动取 `custom_ammo_N` 递增，新建后自动选中。
- 出厂库共 6 条：上述 4 条内置 + 「集束导弹」（missile 类，惯性橙焰 + 长灰烟双尾迹）+「炮弹」（bullet 类，爆炸+命中）。

### 7.2 弹丸种类的美术与行为参数

四类弹丸共享"本体贴图 + 程序化特效"框架，差异在默认色与可用板块：

| 类别 | 适配炮塔 | 默认特效色 | 可编辑板块 |
|---|---|---|---|
| bullet 实弹 | 直射 | #F5E9C8 | 贴图 / 尾焰（含烟尾）/ 爆炸 / 命中 |
| shell 炮弹 | 抛射 | #F0A03C | 贴图 / 尾焰（含烟尾）/ 爆炸 / 命中 |
| missile 导弹 | 导弹 | #D9762E | 贴图 / 尾焰（含烟尾、大力喷射、喷口焰门控）/ 爆炸 / 命中 |
| ray 射线 | 射线（光束/点射） | #9AD9C8 | 光束表现 / 命中（hitscan 无弹道飞行，不提供尾焰与爆炸） |

**行为模板**（尾焰 `template`，覆盖顺序：模板默认 < 用户显式参数）：

- `standard` 标准拖尾（缺省）：rate 60、life 0.35s、size 0.06 格、惯性 0.2；
- `inertia` 惯性甩尾：惯性 0.9、life 0.5s——粒子随弹甩出弯曲尾迹；
- `pulse` 火焰脉冲：速率按 1.2Hz 振荡（0.4~1.6 倍）+ 尺寸/亮度闪烁；
- `smoke` 烟雾弥漫：rate 20、size ×2.5、grow 2 膨胀、暗灰色。

**特效生效门控**：组在=生效，未配置即无对应表现（引擎不添加任何默认特效）。爆炸特效仅在引用炮塔配置了爆炸（`blastRadius > 0`）时播放；直射实弹爆炸命中时爆炸与命中特效**同时**播放；抛射/导弹的爆炸命中只走爆炸特效；射线命中特效在光束每个 DoT tick 的端点、脉冲点射每发的命中点触发。

### 7.3 弹丸编辑器 UI

调试面板「弹丸库」页签，左列表 + 右参数窗：

**左侧列表**

- 列表**最上方**为新建输入行（输入名称 + 新建，回车亦可）；
- 每条目显示类别色块、名称，名称下方子行显示类别名；贴图未就绪时子行附红色弱显提示（加载中「…」/「无素材」）；
- 单选切换，选中条目在右侧编辑。

**右侧参数窗**（自上而下）

1. **效果预览**：画布 360×108（深色底 + 弹道参考线，1px = 1px，弹丸贴图按原图尺寸绘制不缩放；窄容器等比缩小不超框）。单「播放/停止」按钮播放**全程动画**：
   - 非射线：弹体从左端飞入（循环后自屏外）向右平飞，按解析参数喷尾焰/烟尾（与战斗同公式，含导弹点火大力喷射过渡——预览起播即点火），到达右端命中点时按配置**同帧**触发命中碎屑与爆炸爆发（火花+烟尘+冲击环），爆发结束停顿 0.9s 后自动循环（每轮重新点火，大力喷射/烟尾窗口按新一轮起算）；
   - 射线：光束发射与实战一致——起射高速伸展（2400m/s 从发射点扫出，粒子端点/命中闪光随前锋）→ 持续 5s → 停火消退 0.25s（长度冻结、光晕渐隐、亮芯收窄到 0%）→ 停顿 0.9s 循环重启；与战场共用引擎常量，两处永不脱节；
   - 门控：尾焰/命中/爆炸至少配置一项才可播（射线恒可播）；未播放时弹体居中静立。预览粒子层与战场同口径（烟尘贴图不加光、其余加法发光、贴图着色缓存），换贴图立即刷新。
2. **基础字段**：名称、类别（四类下拉）、弹丸贴图（素材库「弹丸」分类下拉：默认=按 id 文件夹 / 「无」= 强制几何弹丸 / 库条目）；附素材目录提示 `/res/projectiles/{id}/`。
3. **尾焰**（射线不显示）：整组增删（「+ 添加尾焰」/「删除该特效(回退)」）；模板下拉、颜色+渐变色取色器（一键回默认/无）、速率/寿命/尺寸/惯性/散开/尺寸变化/淡入数值框；**烟尾子组**在尾焰组底部：整组增删 + 寿命/持续/速率/颜色四字段。所有数值框留空 = 用模板/全局默认，占位提示显示当前生效值（所见即生效值）。
4. **爆炸**（射线不显示）：整组增删；底闪时长/火花数/烟尘数/环数/环速/环厚/湍流/速度抖动/寿命抖动/方向偏置/速度继承 + 颜色。
5. **光束表现**（仅射线显示）：整组增删（删除 = 恢复默认搭配）；四个素材选择器按「亮芯层+光晕层」（亮芯在上，锚定「光束」分类，128×32 原尺寸平铺）与「炮口闪光+命中闪光」（锚定「效果」分类）两行排列，分类不符给出黄色提示；光晕色/亮芯色取色器；闪烁/滚动/发射缩放/命中缩放数值框；吸收粒子/散发粒子/烟尘三个粒子子组整组增删（速率/尺寸/颜色，散射另有 0~360° 散发角度——朝射线源方向为 0°，散射粒子带电焊式速度拖尾亮线）。
6. **命中**：整组增删；时长/碎屑数 + 颜色。
7. 非内置条目底部提供「删除该条目」。

全部编辑直改条目 + bump 即时生效并持久化；喷射类炮塔无弹丸（配置 projectile 不生效并提示）。

### 7.4 弹丸命中/爆炸/轨迹特效参数（概述）

- **尾焰 trail**：`template` 行为模板、`color`/`colorEnd`（寿命内变色，缺省仅亮度渐隐）、`rate`/`life`/`size`（粒/s、秒、格）、`inherit`（0~1 初速随弹比例，1 = 完全随弹）、`spread`（发射锥角弧度，缺省 0.6）、`grow`（>0 膨胀 <0 收缩）、`fadeIn`（淡入秒）；`smoke` 烟尾子组见 §6.4。烟团（grow>0）走烟雾贴图非加法渲染，其余粒子加法发光。
- **爆炸 explosion**：`color`、`duration`（矢量底闪时长，缺省 0.4s）、`sparks`/`smoke`（火花/烟尘粒子数，缺省 10/6）、`speedJitter`/`lifeJitter`（初速/寿命随机幅度 0~1，确定性种子）、`turbulence`（烟尘湍流 0~2）、`rings`/`ringSpeed`/`ringWidth`（冲击环 1~4 层、扩散速度系数、线宽 px）、`bias`（方向偏置 0~1：火花向命中方向收束，1 = 完全锥形）、`inherit`（速度继承 0~1：叠加命中弹丸速度）；v2.54 新增 `fireball`（火球 0~2）/`shock`（软边环 0~2）/`flash`（瞬时照明 0~1）/`streak`（火花拉丝 0/1）；火球 + 冲击环 + 照明按爆炸半径缩放。
- **命中 impact**：`color`、`duration`（缺省 0.15s）、`spikes`（碎屑数，缺省 5）：碎屑向外飞溅 + 中心亮点一闪。
- **射线光束表现**：见 §7.5 与第 8 章。

各参数的完整清单与默认值见第 2 章；渲染分层见第 11 章。

### 7.5 射线（ray）条目的光束表现

射线类条目以 `beam` 组承载光束分层表现（射线类炮塔经 `art.projectile` 引用本条目后生效）：

- **分层贴图**：光晕层 + 亮芯层贴图沿光束**原尺寸平铺**滚动（不缩放拉伸），加法发光、亮度闪烁（`flicker` 0~1，缺省 0.15；0 = 不闪烁）、`scrollSpeed` 贴图滚动（美术 px/s，按格宽缩放，缺省 96；0 = 静止）；缺省贴图 beam_glowA / beam_coreA（素材库「光束」分类），`'none'` = 该层回退程序化纯色矩形。
- **闪光**：炮口闪光（发射期间）与命中闪光（光束端点/点射命中点）贴图缺省 glow16.png，`'none'` = 不显示；`muzzleScale`/`impactScale` 尺寸倍率（缺省 1 = 100%）。
- **三组可选粒子**（组在=生效）：吸收粒子（发射点环带向心汇聚，缺省 12 粒/s · 亮芯色 · 0.05 格）/ 散发粒子（命中点飞溅，缺省 24 · 光晕色 · 0.05 格；`angle` 散发全锥角 0~360°，朝射线源方向为 0°，缺省 360 全向；粒子一律带电焊式拖尾亮线）/ 烟尘（命中点慢速上飘，不加光，缺省 6 · #3A3632 · 0.1 格）。
- 战场光束、弹丸库预览、炮塔预览三处同口径生效；命中粒子由 ray 条目 `impact` 参数在光束 DoT 端点/点射命中点驱动（未配置不产生事件）。
## 8. 射线炮（beam）

射线类炮塔 `type: 'beam'`，与实弹类的根本区别是**无弹道飞行**：命中判定在发射当帧完成（点射）或按持续 tick 完成（光束）。光束的分层贴图美术表现统一由弹丸库 ray 类条目的 `beam` 表现组提供（详见第 7 章 §7.5），炮塔经 `art.projectile` 引用 ray 条目即生效；本章描述炮塔侧的行为与参数语义。

### 8.1 射线子模式 rayMode

`TurretDef.rayMode?: 'pulse' | 'beam'` 仅对 `type:'beam'` 有意义，缺省视为 `'beam'`。现行设计共两种子模式：

| 子模式 | 攻击方式 | 宽幅 | 充能 | 耗电 | 转向 |
|---|---|---|---|---|---|
| `beam` 光束 | 宽幅矩形持续伤害：发射期每个 DoT tick（`dot.interval` 秒）对矩形内全部敌人结算 `dot.damage` | 有（`beamWidth`） | 支持（`chargeTime` 前摇，每次持续发射起射前） | `energyPerShot` 起射一次性扣除（不足则不开火）+ `energyPerSec` 每秒维持（耗尽立即停射） | 持续发射期间转向速度 ×0.5（`BEAM_TURN_FACTOR`），不锁定 |
| `pulse` 脉冲点射 | 即时单体命中：发射当帧对锁定目标造成 `damage`，不可穿透、无弹道 | 无 | 支持（每轮起射前） | `energyPerShot` 按发扣除（多管齐射时 ×实际发数），无维持电量 | 发射期间正常转向 |

- **光束**走持续型武器状态机（与喷射同径）：起射 → 持续 `attackDuration` 秒（缺省 1）→ 停射进入冷却 `cooldown = max(reload ?? 0, fireRate)`；目标丢失、维持电量耗尽、资源标签门控或堡垒过热均立即停射。
- **点射**走实弹类「轮 + 连发」状态机（`fireRate` 秒/轮、`burst`/`burstInterval`、多管 `barrelMode` 均适用）。
- 两者的弹道阻挡口径一致：阻挡弹道的物体可截断射线（阻挡规则见第 5、6 章）。光束在首个阻挡物体格截断，每个伤害 tick 同时对截断物体扣 `dot.damage` 耐久；点射在炮口→目标视线被截断时改为命中该物体并扣除 `damage` 耐久。
- 出厂实例：磁轨光束塔（`rayMode:'beam'`，宽幅 10m、持续 3s、DoT 15/0.5s、充能 2s、发射电 15 + 维持电 5/s）；脉冲射线塔（`rayMode:'pulse'`，单发 26、1.25s/轮、发射电 6/发、产热 9/发）。

### 8.2 光束几何参数

- **宽幅 beamWidth（米）**：米→像素换算为 `beamWidth / 25 格 × cell px`（1 格 = 25m，基准格 30px，战场缩放时 `cell` 随缩放变化）。命中判定半宽 = `m2c(beamWidth ?? 2) / 2`（未配置时判定回退 2m），敌人体型尺寸计入判定宽度。渲染端未配置宽幅时按 8m 回退（仅几何回退口径）。
- **光束长度**：`beamMarch` 从炮塔中心沿炮口方向以 0.2 格步进推进，到达首个阻挡弹道物体格时截断（退回一个步长并记录截断物体），上限 `rangeMax`；命中判定为矩形覆盖（沿程 0~len、横向 ≤ 半宽 + 敌人体型）。
- **起射伸展**：光束起射时记录起射时刻 `beamOnAt`，光束以 `BEAM_ON_SPEED = 2400 m/s` 从炮口高速伸展（250m 射程约 0.10s 到位），当前长度 = `min(截断全长, 已伸展距离)`；伸展期间 DoT 计时器不消费，伸展到位（或抵达阻挡物）当帧立即结算首次伤害；伸展前锋未触及的物体不扣耐久。停火时清除起射锚点。渲染（光束本体/光晕/亮芯/端点闪光）与伤害结算共用同一长度函数，视觉与伤害范围始终一致。
- **分层结构**：光束由**光晕层**（宽 = 宽幅，alpha 0.45 × 亮度系数）与**亮芯层**（宽 = **宽幅 × 0.5**，alpha 0.9 × 亮度系数）叠加而成，均加法发光（`'lighter'`）；染色缺省光晕 `#78C8DC`、亮芯 `#F0FAFF`，亮度按 `flicker` 幅度在 `[1−flicker, 1]` 内双频正弦闪烁（相位按炮塔 id 错开）。层素材缺省 `builtin:beam/beam_glowA` / `beam_coreA`，配置 `'none'` 或素材未就绪时该层回退为同颜色、同宽度的程序化纯色矩形。
- **端点与炮口**：炮口光球（缺省 glow16，尺寸 = 宽幅 × 2 × `muzzleScale` × 脉动）、命中点闪光（缺省 glow16，尺寸 = 宽幅 × 2.6 × `impactScale` × 高频脉动，位于光束端点）；另可配置吸收（发射点向心汇聚）/散发（端点飞溅）/烟尘（端点慢速上飘不加光）三组粒子，组配置在 = 生效。
- **起点定位**：配置了 `art.barrels` 挂点表时，光束/喷射的渲染起点对齐 barrel 0 的炮口点；未配置时为炮塔中心（逻辑判定始终以炮塔中心起算）。

### 8.3 充能帧条与起射前摇

- `chargeTime`（秒）为每次开火周期起射前的前摇（光束/喷射为每次持续发射前；实弹/点射为每轮前；轮内连发/齐射不重复充能）。充能期间不射击、不耗弹药/电量/产热；目标丢失或移出射程射界立即取消充能，重新索敌后重新充能。
- 充能素材为 `art.charge` 配置的**帧条**：按 `frames` 将素材横向等分（帧数钳制 1~12），从左到右顺序播放一遍、不循环；`offset` 为相对轴心的偏移（格，右 +x、上 +y）；`asset` 为素材库引用，`'none'` = 不播放，缺省回退素材文件夹 `charge.png`。
- **末帧语义**：前 N−1 帧占满 `chargeTime`，最后一帧恰在充能结束时点亮；点亮后再经 0.05s 末帧滞留（`CHARGE_LAST_HOLD`，不计入 `chargeTime`）才起射。持续型武器在攻击持续时间内（`firing` 且 `firingLeft > 0`）充能末帧**定格常显**，作为"正在发射"的持续指示。

### 8.4 停火消退 beamFades

光束停火（持续结束/目标丢失/资源中断/过热的统一 `firing` 下降沿）推入一条消退段：快照停火瞬间的起点、角度、当前光束长度与宽幅，存活 `BEAM_FADE = 0.25s`。消退段保持角度与长度不变，透明度按 `alpha = p^0.7`（p 1→0，前期更亮）渐隐，宽度随 p 收窄至 0；宽幅已配置时收窄量已并入层宽，亮芯层贴图另按 vScale = p 纵向收窄到 0% 且保持纵向居中。消退段沿用发射时的光束美术配置；点射不触发消退（其曳光 ttl 0.07s 自行消散）。

### 8.5 光束贴图平铺与缩放规则（drawBeamLayer）

光束贴图在局部坐标系（原点 = 光束起点，+x 沿光束方向）内绘制：

- **平铺不缩放**：贴图按原生尺寸沿轴向无缝平铺，块宽 = `img.width × texScale`、块高 = `img.height × texScale × vScale`，其中 `texScale = cell / 30` 适配战场缩放（30px = 基准格）；端部不足一块时按源矩形裁切。
- **滚动**：`scrollSpeed`（美术 px/s，缺省 96，0 = 静止）乘以 `cell/30` 换算后随时间累计，按块宽取模作为平铺相位偏移，形成沿光束方向的流动感。
- **宽幅驱动缩放（vScale）**：`beamWidth` 已配置时，贴图块高度经 vScale 缩放到目标层宽（光晕 = 宽幅换算像素、亮芯 = 宽幅 × 0.5 换算像素），与几何回退同宽；未配置宽幅时 vScale = 1，贴图保持原生高度。停火消退段复用 vScale 表达亮芯层收窄。
- **回退**：层贴图缺失（`'none'` / 加载失败）时该层绘制程序化纯色矩形，颜色/宽度与贴图层口径一致；块高收窄到 0.5px 以下跳过绘制。
- **脉冲点射曳光**：复用同一套分层平铺绘制（光晕 + 亮芯 + 端点命中闪光），宽度固定 `cell × 0.4`、与炮塔宽幅无关，ttl 0.07s 内随存活比例渐隐；素材全部不可用时回退双描边细亮线（外层青色 2.5px + 内芯白色 1px）。

## 9. 近战与生产炮塔（melee / produce）

### 9.1 近战炮塔：现行设计中不存在

炮塔武器类型枚举 `WeaponType = 'direct' | 'lob' | 'missile' | 'beam' | 'spray'` 不含近战类型，`TurretDef` 无近战相关字段，`TURRET_DEFS` 出厂配置中也没有任何近战炮塔；引擎与渲染层均无近战攻击分支。因此炮塔编辑器不提供"近战"类型选项——本章保留仅为明确该口径。如未来加入近战炮塔，需同时扩展类型枚举、伤害结算与编辑器面板。

### 9.2 生产类：内部模块而非炮塔

生产行为不挂在炮塔体系，而是移动堡垒**内部模块**的属性：`ModuleDef.produce?: { kind: AllyKind; interval: number; cap: number }`（周期产出友军单位，`cap` = 本模块产出单位同时存活上限）。出厂生产模块：

| 模块 | 产出 | 间隔 | 存活上限 |
|---|---|---|---|
| 机器人模块 | 士兵 | 8s | 6 |
| 坦克制造模块 | 坦克 | 15s | 3 |
| 无人机模块 | 战斗机 | 20s | 2 |

结算规则（引擎每帧推进，备战/交战阶段均生效）：

1. 模块放置时计时器初始化为 `interval`（放置即开始倒计时）；
2. 倒计时到点后先统计本模块产出且仍存活的友军数（按 `producerId` 归属），达到 `cap` 则计时器重置为 0.5s 稍后复查，不产出；
3. 未满员则重置计时器为 `interval / 生产特殊格倍率`（模块覆盖堡垒内部「生产」特殊格时倍率为 1.5，即间隔 ÷1.5），并在堡垒前沿出生点（堡垒前缘，不低于敌人刷新区下界）生成对应 `ALLY_DEFS` 单位。

产出的友军单位全图索敌巡航、进入射程后按自身 `damage`/`interval` 攻击，可被敌人击毁（阵亡时飘字「××阵亡」），不占炮位、不耗堡垒资源。友军单位参数（士兵/坦克/战斗机）见 `ALLY_DEFS`。

## 10. 命中效果与伤害表现

### 10.1 爆炸伤害（blast 参数组）

炮塔侧爆炸由两个字段表达：

- `blastRadius?: number`——爆炸半径（米），未配置或 ≤0 = 该武器不爆炸；
- `blastEffect?: BlastEffect`——`{ damage: number; burn?: { damage; interval; duration } }`：爆炸附加伤害与命中燃烧（燃烧为持续伤害：每 `interval` 秒结算 `damage`，持续 `duration` 秒，挂载到目标 dot 队列）。

爆炸结算（`explode`）规则：

- **范围判定**：敌人中心距爆心 ≤ 半径（米转格）+ 敌人体型尺寸即被波及；**伤害无距离衰减**，范围内一律全额 `(爆炸基底伤害 + blastEffect.damage) × 等级系数`，随后附加燃烧 dot；
- **遮挡豁免**：爆心→目标之间存在阻挡弹道的物体（格级视线按 0.2 格步进采样，爆心近场 0.5 格内不计以避免"自我遮挡"）时，该目标豁免本次爆炸；
- **物体波及**：矩形物体按中心 + 半尺寸判定，命中即足额摧毁伤害（`hp = -1` 的不可破坏物体豁免）；油桶被波及摧毁时产生燃烧区域与爆炸圈；爆炸不伤己方墙段；
- **结算顺序**：先登记爆炸视觉事件，再逐敌人结算（遮挡判定 → 伤害 → 燃烧挂载），最后逐物体波及结算。

爆炸触发点按武器类型区分：

| 武器 | 触发点 | 半径 | 伤害基底 |
|---|---|---|---|
| 抛射（lob） | 落点 | `blastRadius ?? 15` | 弹丸伤害（直击并入爆炸一次性结算） |
| 导弹（missile） | 命中点/锁定落点 | `blastRadius ?? 20` | 弹丸伤害 |
| 直射（direct） | 命中敌人处 / 命中阻挡物体处（仅 `blastRadius > 0` 时） | `blastRadius` | 0（直击伤害已按穿透衰减另行结算，波及目标只吃爆炸） |
| 炮塔毁坏 | `onDestroyBlast`（`BlastEffect & { radius }`）在炮塔位置触发 | 配置值 | 配置值 |

### 10.2 命中事件与冲击视觉反馈

- **impacts 命中事件**：实弹命中（爆炸命中时与爆炸事件同时推入）、光束 DoT 端点（每个伤害 tick）、脉冲点射命中点均推入命中事件（ttl 0.15s，携带弹丸条目 id）。实弹命中点取敌人身体视觉中心（脚底锚点上方体型 × 精灵高系数）再加每次不同的确定性抖动（半径 ≤ 体型 × 0.6），保证次次位置有差异。
- **命中特效解析**：由弹丸库条目的 `impact` 参数驱动（`color`/`duration`/`spikes`）——首见时喷射 `spikes` 个短寿命碎屑飞溅粒子（缺省 5 个、颜色缺省弹丸类别色），随后中心亮点一闪（矢量渐隐，`duration` 缺省 0.15s）。条目未配置 `impact` → 不产生事件、不播放任何命中特效。
- **受击闪光**：敌人每次受伤获得 0.08s 受击闪白（`hitFlash`），是所有伤害（含爆炸、DoT、光束 tick）统一的即时反馈。
- **爆炸视觉**：配置了爆炸（`blastRadius > 0`）且引用弹丸条目含 `explosion` 参数组时，播放粒子爆炸——v2.54 统一画法：径向渐变火球 + 软边冲击环（`shock=0` 回退细描边环）+ 瞬时照明光晕 + 拉丝火花/烟尘两组粒子（初速、尺寸按爆炸半径缩放，支持方向偏置与命中弹速继承）；其余情况回退程序化爆炸圈（橙黄填充 + 描边，0.35s 扩散渐隐）。堡垒毁灭演出爆炸（`ExplosionFx.kind`）v2.54 起复用同一画法（固定橙金配色，不读弹丸库）。
  - **v2.55 画法共用层 `fxDraw.ts`**：粒子池绘制（`drawParticlePool`）、爆炸矢量层（`drawExplosionLayers`）、命中闪光（`drawImpactFlash`）及染色缓存/hex 工具统一迁至 `src/game/fxDraw.ts`，战场渲染（render.ts）与编辑器弹丸预览（AmmoPreview）均调用同一组函数——设定依据：v2.54 爆炸升级曾漏同步预览（平行实现漂移），画法收敛到单点后此类事故结构性消除；驱动层仍各自保留（战场 = 游戏事件 + fxSeen 首见 spawn；预览 = fxTick 状态机），仅画法共用。炮塔美术预览（drawArtPreview/drawPreviewRack）因坐标系为像素空间且涉及炮位布局叠加层，统一工作见待开发记录 #15。

### 10.3 击杀飘字与伤害数字

现行设计**不做逐次伤害数字跳字**；飘字仅用于事件性提示，统一由 `floats` 队列承载（ttl 0.8s，金色 `#D9A441` 粗体描边字，随存活时间上移约 16px 并渐隐）：

- **击杀赏金**：敌人 hp ≤ 0 进入击杀结算——金币 `+bounty`、击杀数 +1，并在敌人位置飘字「+N」；
- **其他事件飘字**：「炮塔损毁」「建筑被毁」「××（友军）阵亡」。

### 10.4 表现分层

命中与伤害表现在战场渲染顺序中位于：炮口火光 → 射线/喷射（含光束分层贴图与端点闪光）→ 光束停火消退段 → 爆炸（粒子/程序化圈）→ 命中特效（碎屑 + 亮点）→ 粒子层（尾焰/烟尘/吸收散发等）→ 曳光与脉冲射线 → 飘字。各层的绘制次序、画布合成方式（加法发光/普通）与战场缩放适配的完整规范见第 11 章「渲染分层」。
## 11. 美术与渲染分层

本章描述炮塔/弹丸的贴图分层模型、堡垒特效点粒子系统、整帧渲染分层顺序，以及素材库（assetlib）的组织与分发约定。实现位于 `src/game/art.ts`、`src/game/fortressFx.ts`、`src/game/particles.ts`、`src/game/render.ts`、`src/game/assetlib.ts`，配置类型位于 `src/game/config.ts`（`TurretArt` / `FortressEffectPoint` / `ProjectileArtDef`）。

### 11.1 炮塔贴图结构（分层合成）

炮塔视觉按**层**合成，每层独立解析素材、独立降级。层清单（`TurretArtAssets`）：

| 层 | 字段 | 行为 |
| --- | --- | --- |
| 底座 base | `art.baseAsset` | 居中于占格；挂载炮塔随船体朝向旋转，地面炮塔不旋转。缺省「无」（`'none'`）不绘制底座、塔体直接落地；`'geo'` 强制几何色块 |
| 炮身 turret | `art.turretAsset` | 以 `art.anchor`（占格归一化坐标，缺省 `[0.5,0.5]`）为旋转轴心，随瞄准方向旋转 |
| 炮管 barrel × N | `art.barrelAsset` | 每管一根，根部锚定挂点 `mount`；`'none'` = 无炮管层（无管发射井/能量塔造型），挂点/炮口/火光逻辑不受影响 |
| 开火效果 flash | `art.flashAsset` | 可选；`'none'` = 不播放火光。表现硬编码：横向帧条 2 帧 × 每帧 0.05s（共 0.1s）、逐帧缩放 1.4×→1×、加法发光（`lighter`）；宽 < 高×帧数的单张图整图绘制、逐帧脉冲缩放 |
| 辉光 glow | 文件夹 `glow.png` | 可选；默认仅过热时显示（`art.glow.overheatOnly`，缺省 true） |
| 充能 charge | `art.charge.{asset,offset,frames}` | 可选帧条：按帧数横向等分（1–12 帧）、从左到右顺序播一遍，帧时长 = 充能时间/帧数；充能结束末帧短暂滞留后在攻击持续期内定格常显。`'none'` = 不播放，缺省走文件夹 `charge.png` |

**分层就绪规则（逐层降级）**：`turretArtState()` 返回 `loading / ready / fallback`。底座/炮身/炮管三层中任一层 ready 即进入贴图渲染——ready 层画贴图、缺失层由渲染侧几何补绘；有层仍在加载则整体 loading（等全部落定，预览重试机制依赖该态）；三层全部缺失（未配置素材）则整体几何回退，与未配置素材的默认炮塔视觉一致。flash/glow/charge 为可选件，缺失仅无对应效果，不参与 ready 判定。

**每层素材源解析**（`turretLayerSrcs()`，纯函数，按优先级取第一个加载成功的候选）：

1. 素材库引用：`art.baseAsset/turretAsset/barrelAsset/flashAsset` 为素材库条目 id 时，经 `assetImage()`（按条目 id 缓存）取条目 src；
2. 文件夹约定：`/res/turrets/{folder}/{part}.png`，folder = `art.spriteSet`（兼容字段，UI 不提供选择）缺省回落为炮塔 id；
3. 特殊值 `'none'`（显式不绘制该层）/ `'geo'`（强制几何绘制）；引用不存在 → 该层缺失。无通用兜底集：无素材即几何绘制。

多炮塔共享同一文件夹时缓存条目按实际文件夹名合并；同一图片 src 多塔共享 `srcCache`。sim/node 无 DOM 环境静默走 fallback，不发起加载。

**挂点表 `art.barrels`**：每项 `{ mount, muzzle }`，坐标相对轴心、单位格，基准系为「炮口朝上」：x 向右为正、y 沿炮口方向为正。`mount` 是炮管根部锚点，`muzzle` 是弹丸出生点与火光定位点。挂点表数量应与逻辑炮管数（`def.barrels`，缺省 1）一致；不一致时**渲染/出生点以挂点表为准**，`validateArt()` 给黄色警告。未配置挂点表时引擎按逻辑炮管数自动生成：单管 = 炮塔中心、炮口前伸 0.35 格；多管沿垂直炮口方向均布（展开宽度 = 占格宽 × 0.6）。后坐为统一参数 `art.recoil`（全管共用，缺省 0.1 格，0 = 无后坐动画），击发后炮管在 2× 火光时长内线性回位。

**多管与双管示例**：发射模式 `barrelMode` 分齐射（salvo，全管同时）与轮流（sequential，按 `burstInterval` 轮转各管，`barrelIdx` 推进）。出厂哨戒机枪（`mg`）即双管轮流：炮身贴图引用内置素材 `DualGun-_S1`、炮管贴图引用 `DualGun-_S2`，`barrels: 2 / barrelMode: 'sequential'`，挂点表给出左右两个挂点（(-0.1, 0.25) / (0.1, 0.25)）与对应炮口，`zBias: -1` 使炮管层压在炮身层之下（根部被炮身遮盖）。`zBias < 0` = 炮管在炮身之下，`>= 0`（缺省）= 炮管在上。

**导弹挂载显示**：导弹塔（`type: 'missile'`）默认在炮管旁显示待发弹架（`art.rack.show`，缺省 true；`rack.dx/dy` 为挂载点相对炮管挂点的偏移，缺省 0 / 0.12 格）。待发弹逐发消耗、打空显示空架、装填完成逐枚复挂（复挂弹带 0.25s 推入渐显动画，仅作用于最新一枚）；轮流模式下从当前管侧优先消耗。弹体用所引用弹丸条目的本体贴图，无贴图回退类别色几何小导弹。

**尺寸与层级（zLevel）**：所有贴图按原始像素尺寸绘制，基准格统一 `BASE_CELL = 30px`——缩放 1 时 1 贴图像素 = 1 画布像素，横竖屏/任何设备贴图与网格比例恒定，随用户缩放等比变化；像素风一律最近邻采样（关闭平滑）。战场上多座炮塔重叠时的叠放次序由渲染层级键决定（`turretRenderKey()`，升序绘制、靠后叠在上，稳定排序保持同键放置先后）：

1. 第一键 = 型号 rank：**尺寸越小越底层**（S=0 < M=1 < L=2）；
2. 第二键 = 挂载炮位的 `zLevel`（缺省 1，越大越上层）；地面炮塔视为 1。

**校验**：`validateArt(def)` 输出 `errors`（硬性错误，编辑面板红标）与 `warnings`（提示，黄标）。硬错误包括 anchor/挂点/炮口非数值对、后坐为负、充能帧数越界（1–12）、光束组颜色非 `#RRGGBB`、缩放非正数、粒子组速率/尺寸非法等；警告包括挂点数≠炮管数、喷射塔配弹丸引用（不生效）、弹丸类别与炮塔类型不匹配（允许跨类借用，按配置生效）、引用不存在的素材条目（回退程序化/几何表现）、配了充能动画但未配充能时间等。

### 11.2 光束与弹丸表现（美术侧口径）

**光束分层**（远行星号式，配置挂在弹丸库 `ray` 条目的 `beam` 组上，炮塔经 `art.projectile` 引用解析，`beamArtConfigOf()` 为无炮塔上下文的复用入口）：

- 光晕层（glow）+ 亮芯层（core）：贴图沿光束轴向平铺滚动（`scrollSpeed`，美术 px/s，缺省 96）、加法发光、亮度闪烁（`flicker` 0~1，缺省 0.15）；染色 `fringeColor`（缺省 `#78C8DC`）/ `coreColor`（缺省 `#F0FAFF`）；白图 alpha 遮罩经 `tintedFx` 预着色缓存复用。层素材 `'none'`/未就绪 → 该层回退程序化矩形。
- 炮口光球（muzzle）与命中点闪光（impact）：缺省 `glow16`，尺寸分别 ×`muzzleScale`/`impactScale`（缺省 1），高频脉动、加法发光。
- 宽幅联动：炮塔配了 `beamWidth`（米）时贴图高度缩放到 宽幅/25 格（光晕=宽幅、亮芯=宽幅×0.5）；未配 = 贴图原生高度。
- 三组粒子（组在 = 生效，颜色缺省随本组配色）：吸收 absorb（发射点环带向心汇聚）、散发 scatter（命中端点飞溅，加法发光 + 电焊式拖尾亮线；`angle` 为朝射线源方向 0° 的全锥角，0–360°，缺省 360 全向）、烟尘 smoke（命中端点，膨胀不加光）。
- 停火消退：保持停火角度/长度，宽度收窄 + 渐隐（alpha = p^0.7，亮芯收窄到 0%）。脉冲点射（rayMode 'pulse'）的曳光与持续光束共用同一分层贴图配置。

**弹丸条目美术**（`ProjectileArtDef`，详见弹丸库章节）：本体 `projectileAsset` 解析链与炮塔分层同构（素材库引用 → `/res/projectiles/{folder}/projectile.png` 文件夹；`'none'` = 显式几何弹丸），本体贴图为 ready 必需件，trail/explosion/impact 文件夹帧条为可选回退件。尾焰/爆炸/命中一律以**程序化参数段**为准（`resolveTrailFx/resolveExplosionFx/resolveImpactFx`，段在 = 生效）：尾焰四模板（standard 标准 / inertia 惯性甩尾 / pulse 火焰脉冲 / smoke 烟雾弥漫，参数覆盖顺序 = 模板默认 < 显式参数），可叠加长存留烟尾子组（`trail.smoke`，可选「持续」窗口，战场侧按引用炮塔燃烧时间钳制）；爆炸段含火花/烟尘双组粒子 + v2.54 统一矢量层（径向渐变火球 + 软边/描边冲击环 + 瞬时照明，火花可拉丝）；命中段为碎屑飞溅 + 中心亮点一闪。未配置特效段 = 无任何默认效果（无尾焰配置即无尾迹、无喷口焰）。

### 11.3 特效点系统

堡垒特效点（`FortressDef.effects: FortressEffectPoint[]`）是**贴附船体的程序化粒子发射点**，仅视觉、不参与碰撞，按堡垒停止/移动状态切换播放。发射位置随船体位姿（位置 + heading 旋转，与渲染堡垒上下文同一变换）；**粒子离口即在世界空间独立运动**，已散发的烟/尘不再跟船。

配置字段：

| 字段 | 含义 |
| --- | --- |
| `id` / `x` / `y` | 标识与堡垒局部坐标（格，可为小数） |
| `kind` | 粒子种类：smoke 烟雾 / flame 火焰 / dust 尘土 / spark 火花 |
| `state` | 播放门控：idle 停止时 / move 移动时 / both 始终 |
| `layer` | 渲染层级：ground 地面层 / air 空中层（缺省：dust=ground，其余=air） |
| `rate` | 发射速率（粒/s，缺省按 kind） |
| `size` | 粒子尺寸（格，缺省按 kind） |
| `life` | 粒子寿命（秒，缺省按 kind） |
| `inherit` | 堡垒速度继承 0~1（缺省：dust=0.3 被车带起前扬，其余=0 不随车） |

各 kind 缺省口径（`KIND_DEFAULTS`）：smoke（air，8 粒/s，0.12 格，1.3s）、flame（air，24，0.08，0.28s）、dust（ground，14，0.1，0.9s，inherit 0.3）、spark（air，7，0.05，0.22s）。运动形态按 kind 程序化（不开放方向参数）：烟雾 = 上升扩散烟团（膨胀至半寿命后冻结尺寸渐隐）；火焰 = 短寿命加法火焰（金→橙渐变 + 高频闪烁）；尘土 = 横向弥散 + 按 inherit 继承车速；火花 = 随机方向短促亮线（电焊拖尾）。发射按 rate 累加器跨帧连续累计（避免小数粒截断闪烁），各点以坐标 hash 错开首粒相位。

**粒子池双通道**：渲染端持两个粒子池——`groundPool`（地面层：尘土等，绘制于地形之上、堡垒底座之下）与 `fxPool`（空中层：烟雾/火焰/火花、弹丸尾焰、爆炸、光束粒子等，绘制于弹丸与爆炸之后的最上层附近）。发射按 `layer` 路由入池。粒子系统为渲染端纯视觉、纯函数可 sim：每池固定上限 `PARTICLE_CAP = 400`，超出回收最老粒子；逐帧运动（位置积分 + drag 减速 + 膨胀/收缩 + 湍流 + 寿命衰减）由 rAF wall-clock dt 驱动（clamp 0.1s 防后台切回大步长），编辑模式不发射特效点粒子。绘制口径：`grow > 0` 的膨胀粒子（烟尘类）走 smoke32 贴图、正常混合不加光；其余走 particlealpha32 贴图 + 预着色 + 加法发光；颜色渐变按寿命进度 color→colorEnd 量化 8 档缓存。

### 11.4 渲染分层总表

`draw()`（`src/game/render.ts`）整帧绘制顺序，自底向顶（画布内容；HUD/卡片栏等 DOM UI 在画布之上，不在此列）：

| 序 | 层 | 内容 |
| --- | --- | --- |
| 1 | 地面底层 | `ground_base.png` 32×32 平铺满可视区（未就绪回退统一灰底） |
| 2 | 战场层 | `ground_mid.png` RMXP Autotile 笔刷层（出生带不铺设） |
| 3 | 基地层 | `ground_top.png` 基地格全集（含墙段格垫底） |
| 4 | 基地内角凹转角 | wall01 图集 22 系贴图叠加 |
| 5 | 网格线 | 半透明细线 |
| 6 | 地形 | 贴地效果层（水坑等），永不挡弹道/移动 |
| 7 | 物体 | 油桶/废墟/岩石（含受损血条） |
| 8 | 燃烧区域 | 双层圆斑 |
| 9 | 围墙段 | ground01 地面块垫底 + wall01 九宫墙体块 + 损伤裂纹 + 段耐久条；缺口格画瓦砾 |
| 10 | 固有建筑 | 色块 + 名称 + 血条 |
| 11 | 履带印 | 压暗瓦片压印，前段全亮末段渐隐 |
| 12 | 地面粒子层 | `groundPool`（特效点尘土等） |
| 13 | 移动堡垒 | 随 heading 旋转的复合体：底座层（贴图 > 自由形状逐格 > 矩形兜底）→ 履带/轮子层（底座之上、主体之下，不随俯仰）→ 内部模块层（内部建造模式：格阵 + 模块 + 幽灵）或主体层（主体贴图 + 船艏标记 + 可见炮位圈/视界弧）；船体耐久条为屏幕空间不随旋转 |
| 14 | 炮塔 | 按渲染层级键升序（§11.1：S<M<L、同尺寸 zLevel 大者在上）；挂载炮塔随主体俯仰平移；内部建造模式不画 |
| 15 | 敌人 | 像素精灵 + 血条 |
| 16 | 友军单位 | 士兵/坦克/战斗机（飞行单位带偏移阴影） |
| 17 | 弹道/弹丸 | 贴图弹丸按航向旋转原尺寸绘制；抛射带影子与抬升；导弹喷口 glow（随尾焰配置门控）；尾焰/烟尾粒子在此发射入 fxPool |
| 18 | 炮口火光 | 击发时刻炮口点 flash 帧条，加法发光，朝向锁定击发时刻 |
| 19 | 射线/喷射 | 持续光束分层（光晕+亮芯+炮口光球+命中闪光）与光束三组粒子发射；喷射扇形；持续发射期炮口 flash 循环 |
| 20 | 光束停火消退 | 宽度收窄 + 渐隐 |
| 21 | 爆炸 | 程序化段（矢量底闪 + 冲击环，火花/烟尘粒子首见发射一次）；未配置回退程序化圈 |
| 22 | 非爆炸命中 | 碎屑飞溅 + 中心亮点一闪；未配置不播放 |
| 23 | 空中粒子层 | `fxPool`（尾焰/爆炸/命中/光束/特效点空中粒子） |
| 24 | 曳光/脉冲射线 | tracer 线段（脉冲点射共用光束分层贴图） |
| 25 | 建造预览 | 放置/铺墙幽灵（合法绿/非法红 + 原因小字） |
| 26 | 场景编辑叠加层 | draft 内容（含编辑态炮塔：复用 `drawTurret` 伪实例，与战斗态表现一致） |
| 27 | 飘字 | 伤害/提示文字上浮渐隐 |
| 28 | 滚动提示 | 右缘迷你滚动条（屏幕空间） |

### 11.5 素材库（assetlib）

素材库（`src/game/assetlib.ts`）是统一的图片条目注册表，供炮塔分层、弹丸本体、光束、模块、履带/轮子等各处按 id 选配引用。条目结构 `{ id, name, src, builtin, category }`，src 为 dataURL（上传）或 `/res/` 文件路径（内置）。

**分类**（`AssetCategory`）：base 底座 / turret 炮身 / barrel 炮管 / flash 效果（开火火光、命中/炮口闪光等特效贴图）/ projectile 弹丸 / charge 充能 / beam 光束 / module 模块 / other 未分类。选配下拉**严格按分类过滤**（选底座时不显示底座以外素材；未分类条目不出现在任何选配下拉，可在素材库页签改分类）；当前引用与分类不符时仅给黄色提示，不作硬拦截。

**/res/ 目录结构**（`public/res/`，按分类组织）：`beam/`（光束贴图）、`fortresses/`（堡垒底座/主体）、`fx/`（粒子特效：glow16/particlealpha32/smoke32）、`ground/`（三层地面）、`library/`（内置常用素材）、`projectiles/`（弹丸）、`turrets/`（炮塔分层文件夹）、`walls/`（城墙图集）、`zombies/`（丧尸精灵）。代码内出现的 `/sprites/` 前缀引用会被 `resCompatUrl()` 自动重写为 `/res/`，存量口令/存档中的引用不失效。

**两类条目**：

- **内置条目**（`builtin: true`）：代码注册、随应用分发，**不可删、不持久化、不随口令导出**。当前内置 = 常用素材组（`builtin:library/{文件}`：炮身/炮管/弹丸/充能/开火效果/履带瓦片等，展示名可与文件名不同）+ 光束组（`builtin:beam/*`，/res/beam/）+ 特效组（`builtin:fx/*`，/res/fx/）。素材文件夹约定解析（`art.spriteSet`、条目 id 文件夹）的注册机制保留，通用兜底集已废止——无素材即几何绘制。
- **上传条目**（`upload-N`）：本地图片转 dataURL（PNG/JPG，单张超 500KB 给软警告仍允许），`localStorage 'td-asset-lib'` 持久化；可删、可改分类、随口令导出。出厂上传素材以 dataURL 内联在代码中，**仅首次启动播种**（无存储 key 时写入并落盘，此后删除不复活）。无存储环境（sim/node）转为内存播种。

**口令烘焙（概念）**：编辑器中经口令搬运验证成熟的配置，可以两种形式沉淀为随应用分发的出厂内容——① 素材：图片文件落位 `/res/` 对应目录并注册为内置条目（或内联为出厂上传播种），原口令中的上传引用随之转为内置引用；② 定义：炮塔/弹丸参数直接写入 `config.ts` 的 `TURRET_DEFS` / `PROJECTILE_ARTS` 出厂表。烘焙后口令侧无需再携带对应内容；素材贴图文件级替换（只换文件、不动注册/分类/引用）是同口径的后续维护方式。

**导入导出**：口令仅携带上传条目（`uploadsForExport()`）；导入按 id 去重合入并持久化（`importUploads()`），口令无 assets 段时不动现有库。素材图片按条目 id 缓存加载，无 DOM 环境静默 error。

## 12. 炮塔编辑器 UI

炮塔编辑器是 DEBUG 面板（`src/components/DebugPanel.tsx`）的「炮塔」页签；同一面板内还有弹丸库、素材库、地形/物体、战场编辑器、堡垒、模块页签，顶栏常驻导出/导入/重置/关闭。所有编辑都经统一的 `bump()` 通道：刷新界面 + 即时持久化（见 §13.2），修改对新旧炮塔均即时生效。

### 12.1 页签布局

- **新建自定义炮塔**：选武器类型（直射/抛射/导弹/射线/喷射）+ 可留空名称，按该类型原型克隆出 `custom-*` 条目，创建后自动选中，并即刻出现在备战卡片栏可放置试玩。
- **左列表右参数窗**：左侧为炮塔定义单选列表（色块 + 名称 + id，自定义条目带标记）；右侧为当前条目的完整表单。自定义条目可改名、可删除（删除时同步移除场上已放置的同型炮塔）；内置条目不提供删除。
- 参数区与美术配置区横版并排（竖版上下堆叠），两个板块均常驻显示、无折叠。

### 12.2 参数面板（字段显隐规则）

参数表由声明式字段规格数组 `FIELDS` 驱动：每项 `{ path, label, tip, type, step, options, showIf, get, set }`——`path` 支持 `a.b` 嵌套；`tip` 为悬停/长按解释气泡；`type` 分 number / boolean / select；`get/set` 用于派生/复合字段（如「制导模式」下拉读写 `guided` + 清理/保留延迟制导字段、「集束子弹数」读写在填入时自动补默认扇角/近炸距离），提供后读写绕过通用 path 存取。

**显隐规则**：

- 字段带 `showIf(def)` 的，按谓词显隐（多为类型相关：导弹系的制导/集束/燃烧时间字段仅 `type: 'missile'` 显示，充能时间仅射线类显示，射线模式仅 `type: 'beam'` 显示，发射模式仅直射/抛射/导弹与点射线显示等）；`showIf` 不依赖字段自身值，避免编辑中间态（空/0）导致字段自隐藏。
- 不带 `showIf` 的为**可选参数**：字段在定义上有值才显示（删空即隐藏，回归未配置语义）。
- 常驻字段（`showIf: () => true`）：爆炸半径/爆炸附加伤害/燃烧三参、炮管数——保证未预置爆炸的炮塔也能直接填入开通爆炸。
- 数值输入统一最小调整值 0.1（个别字段声明更大步进）；带 `showIf` 的字段可清空（清空 = 未配置），常驻可选字段删空停留于本地编辑、失焦回落。

**型号联动**：型号（S/M/L）下拉决定可挂炮位尺寸并联动占格——S=1×1 / M=1×2 / L=2×2，不提供独立占格字段。

### 12.3 标签编辑器（索敌标签）

`TagEditor` 常驻于参数表下方，编辑 `def.tags`：已配标签以 chips 形式横排展示（文案如「偏好·血最多优先」「约束·不打空中」「资源·弹药低于20%禁火」），每个 chip 带 × 移除，删空即删除 `tags` 字段；有「清空」按钮一键恢复默认索敌。新增区为三段联动：类别下拉（偏好/约束/资源）→ 对应键下拉（偏好八键：近堡垒/近炮塔/血最多/血最少/大单位/小单位/空中/地面优先；约束两键：不打空中/不打地面；资源四键：弹药/电量/热量/防御）→ 资源类再选「低于/高于」与百分比阈值（钳 0–100）。同键（资源类同键同向）重复添加被去重拦截。语义细节见索敌系统章节。

### 12.4 美术配置块与预览

**美术配置块**（`ArtEditor`）：未配置时显示「+ 启用美术配置」（启用即填入默认骨架：轴心居中、底座无、统一后坐 0.1、单挂点表）；已配置时标题栏显示校验徽标（红 ✕=errors 数 / 黄 ⚠=warnings 数，`validateArt()` 口径）。所有修改在结构化克隆上进行、整体写回。表单内容：

- 分层素材下拉 ×3（底座/炮身/炮管）：选项 = 「默认（文件夹/几何绘制）」+ 特殊项（底座另有「无」「几何」，炮管另有「无」）+ 素材库按分类过滤的分组（内置/上传）。
- 弹丸引用下拉（喷射塔不显示）：直射/抛射/导弹标签为「弹丸」，射线类标签为「光束(弹丸)」——引用 ray 条目即接管光束分层表现与命中特效；选项按弹丸四类分组。
- 火光下拉：可选「无（不播放）」；表现参数硬编码不提供编辑。
- zBias（<0 炮管压炮身之下）与统一后坐（写入时清理逐管遗留后坐值，避免双源）。
- 轴心 x/y（0–1 相对坐标）。
- 炮管挂点卡片：数量由「炮管数」决定、不提供手动增删；每卡编辑挂点 x/y 与炮口 x/y；未配置的管显示「默认」并按引擎均布规则预填。
- 导弹塔附加「挂载显示」开关与挂载 x/y 偏移；射线类附加「充能动画」子组（充能 x/y、帧数 1–12、素材下拉）。

**炮塔预览**（`ArtPreview` / `drawArtPreview`）：深色底方形画布，0.5 格细分网格 + 过轴心的坐标轴（x 向右、y 向上=沿炮口），与战场渲染严格共用「30px = 1 格」美术坐标基准——缩放 1 时预览与战场逐像素一致。绘制内容与战场同规则：分层贴图/逐层几何回退、zBias 次序、挂载弹架满挂展示、充能帧条第 1 帧。「标注」勾选框统一显隐轴心（黄）、挂点（蓝，挂1/挂2…）、炮口（红，口1/口2…）、后坐虚线箭头、火光尺寸参考圆、充能点位（绿）。「播放动画」按钮循环播放射击流程：充能前摇（含末帧滞留）→ 击发（火光 2 帧 + 炮管后座回位）→ 按连发数/连发间隔轮转各管，周期不低于开火间隔；光束/喷射为充能后持续开火（光束矩形/喷射扇形 + 火光循环 + 光束三组粒子），击发弹丸从炮口飞出。素材加载中/完成后预览自动升级，无需手动刷新。

核心炮塔画法不再由预览复制实现：`drawTurretPreviewCore()` 调用战场同一套 `drawTurretLayers()`，统一底座/炮身/炮管、逐层几何回退、后座、充能和导弹挂架；预览侧只负责网格、轴心/挂点/炮口标注以及动画流程驱动。

参数区上方的「热管理预览」调用 `simulateTurretHeat()`，按当前出战堡垒的热量上限与散热能力模拟连续射击 20 秒，显示热量曲线、峰值与首次过热时间；模拟为只读，不改动对局。

**光束预览**：光束表现（分层贴图/闪光/三组粒子）在弹丸库 ray 条目上编辑，其预览见 §12.6 的弹丸预览——ray 条目的播放动画即光束预览：起射按速度伸展（端点闪光随前锋）→ 持续 5s → 消退消失（长度冻结、光晕渐隐、亮芯收窄到 0%），停顿后循环，与实战口径一致。

### 12.5 安装/卸载与炮位约束（概述）

炮位的定义与编辑（位置/尺寸/类型限制/视角/层级/隐藏内置）属堡垒域（见堡垒编辑器设计），炮塔侧只消费其约束：

- **安装**：备战阶段按下「炮塔」按钮展开卡片栏（牌面显示名称、型号、造价；负担不起的置灰），点选卡片进入挂炮模式——战场上显示全部可见炮位的槽位圈与视界弧，匹配的空闲炮位（型号相符 `def.mount === hp.size` 且类型在 `hp.types` 白名单内）高亮，点击即挂载（`canMountTurret()` 校验：炮位存在且非隐藏内置、型号匹配、类型兼容、未被占用、费用充足）。挂载初始朝向按炮位视角：全视角 = 船头向 / 指定视角 = 视界中心 / 固定视角 = 固定角。
- **卸载**：拆除工具点中炮塔即卸下并返还半价（仅备战期；堡垒隐藏内置炮位的内置武器不可拆）。
- **视角三模式**在编辑器中的体现：堡垒编辑器炮位条目提供「全视角 / 指定视角 / 固定视角」下拉（指定视角编辑起止角，支持跨 0°；固定视角角度钳 -180~180°，上方 0° 顺正逆负），预览同步显示视界扇形/单射线；战场上选中炮塔时按其炮位视角绘制射界指示——固定视角画单射线、指定视角画相对船头的视界扇形、全视角画整圆，并叠加最小射程盲区与免转瞄准锥（射角）。炮塔定义本身不再有射界字段，地面炮塔 360° 自由旋转。
- **渲染层级**：炮位 `zLevel`（缺省 1）决定同尺寸炮塔重叠时的叠放次序（§11.1）。

### 12.6 弹丸编辑器入口

弹丸编辑并入同一 DEBUG 面板的「弹丸库」页签（`AmmoTab`），操作模式与炮塔页签一致：左列表右参数窗、新建条目（`custom_ammo_N`，缺省几何弹丸）、选中即编辑、修改经 `bump()` 即时生效持久化。条目字段：名称、类别（bullet 直射弹 / shell 抛射弹 / missile 导弹 / ray 射线）、本体贴图选配（素材库弹丸分类 / 「无」= 几何弹丸）、尾焰/爆炸/命中三个程序化特效段（每段可「+ 添加 / 删除该特效（回退）」，段内数值留空 = 用模板/全局默认并显示生效值占位；尾焰段含模板下拉、渐变色与烟尾子组）、ray 条目附加光束表现组（光晕/亮芯/命中闪光/炮口光球素材与颜色、闪烁、滚动速度、双向闪光缩放、吸收/散发/烟尘三组粒子）。**弹丸预览**（`AmmoPreview`）：360×108 深色画布，弹体按原图尺寸静立或随动画飞行；单「播放/停止」按钮播放从左飞行到右端命中/爆炸的全过程循环（ray 条目为光束起射→持续→消退循环）；未配置任何效果的条目不可播（射线恒可播）。炮塔侧的弹丸入口即美术配置块的「弹丸 / 光束（弹丸）」下拉（§12.4）。

## 13. 口令、持久化与模拟回归约定

### 13.1 配置口令（导入/导出）

口令是全部 debug 配置的跨设备搬运格式（`src/game/config_transfer.ts`）：`exportConfig()` 实时取注册表现状打包为 bundle，序列化后 base64 编码（纯 JS 实现，UTF-8 字节 ⇄ base64，unicode 安全，容忍粘贴带入的空白/换行）。bundle 形状：

```ts
{
  app: 'td-config',
  version: 4,
  turretDefs,        // 炮塔定义全量
  projectileArts,    // 弹丸库全量
  level,             // 战场关卡
  assets,            // 素材库上传条目
  fortressLib,       // 堡垒类型库（自定义堡垒 + 出战选择）
  moduleDefs,        // 模块库
}
```

**版本兼容语义**：`version` 接受 1/2/3/4；各段按引入门槛可选——version ≥2 携带 assets、≥3 携带 fortressLib、≥4 携带 moduleDefs；低版本口令缺失的段**不动导入方现有数据**（按 id 去重合入或保持原状），携带的段以口令数据为准。导入的炮塔定义一律经 `applyMountFoot()` 按型号归一化占格。

**导入保证**：解析与形状校验（base64 → JSON → `app` 标识/版本/各段形状）全部通过后，才一次性就地替换注册表并落盘；任何一步失败不写坏现有数据。`parseConfigSmart()/applyConfigSmart()` 支持双格式：以 `{` 开头的文本按美化 JSON 解析（与 `exportConfigJson()` 对应，便于直接编辑），否则按 base64 口令解析，共享同一校验与落盘通道。编辑器顶栏「导出/导入」按钮分别生成口令串小窗与粘贴导入小窗（导入结果显示成功/原因）。

### 13.2 持久化模式

炮塔/弹丸/模块定义采用**内存编辑 + bump 落盘**模式（`src/game/persist.ts`）：

- 注册表即真相：`TURRET_DEFS` / `PROJECTILE_ARTS` / `MODULE_DEFS` 是内存可变数组，编辑器直接改数组元素；任何编辑后调 `bump()`（界面重绘 + `saveAll()`）即时持久化。
- 存储：localStorage 三 key——`td-turret-defs` / `td-projectile-arts` / `td-module-defs`，值均为全量 JSON 信封 `{ version, data }`。素材库上传条目单独走 `td-asset-lib`（§11.5）；关卡走 `td-level-config`；堡垒类型库走独立 key（含自定义堡垒与出战选择，内置堡垒不持久化，覆盖内置 id 即视为自定义覆盖、删除覆盖即恢复出厂）。
- 加载：模块初始化时读取各 key，解析成功即**就地 splice 替换注册表内容**（保持 import 引用有效，引擎/渲染零改动）；无存储环境（sim/node）或解析失败均静默用出厂默认。
- 信封版本：当前 version = 2；读入 version 1 数据时按旧语义迁移（`fireRate` 取倒数，缺失/0 跳过）并回写新信封；其余版本号丢弃用默认。
- 重置：「重置」按钮调 `resetPersistedToDefaults()`——出厂快照（加载替换前留存）就地替换全部注册表并清除存储 key，刷新后不回魂。模块库另有单独回出厂入口。
- 跨设备搬运不经 localStorage，由口令通道（§13.1）完成：导出口令 = 当前内存注册表快照，导入 = 校验后就地替换 + 落盘，与本地持久化共用同一份数据。

### 13.3 模拟回归（scripts/sim.ts）

`scripts/sim.ts` 是无头模拟验证脚本：esbuild 打包后用 node 运行，覆盖各编辑器设计文档的关键验收口径（炮塔/弹丸/堡垒/关卡/素材/口令/粒子/特效点均有用例组）。

**运行方式**（项目根目录为 cwd，素材文件断言按 `public/res/` 相对路径读取）：

```bash
npx esbuild scripts/sim.ts --bundle --platform=node --format=esm --outfile=/tmp/sim.mjs
node /tmp/sim.mjs          # 输出逐条 PASS/FAIL，末尾 ALL CHECKS PASSED
```

末尾汇总打印 `ALL CHECKS PASSED` 或失败计数，有失败即非零退出（汇总在真正末尾，尾部用例失败同样被门禁捕获）。个别素材字节级一致性断言需经环境变量 `TD_TOKEN_FILE` 传入口令文本文件（base64 配置口令，用于比对库内 PNG 与口令内嵌 dataURL）。

**框架约定**：

- **确定性**：引擎逻辑不调用 `Math.random`；精度散布、导弹摆动相位和出怪位置均由 `eventRandom(eventId, stream)` 派生。需要隔离散布的测试应显式把临时武器定义的 `accuracy` 设为 0，并在用例结束后恢复；粒子/特效侧的角度与抖动同样保持可复现。
- **断言**：`check(name, cond, detail?)`——通过打印 `PASS`，失败打印 `FAIL` 并计入失败数；用例名标注所属功能点。
- **驱动**：`run(s, seconds, dt = 0.05, perTick?)` 以固定 dt 推进引擎 `tick()`；`arena()` 构造资源拉满、进入交战态、无炮塔的最小场景；`mkTurret(s, defId, x, y)` / `mkEnemy(s, kind, x, y, hp)` 放置测试实体（敌人钉住一个场外不可摧毁的假物体，不移动）。
- **隔离**：LEVEL 可变，用例间以出厂快照 `restoreLevel()` 无损恢复；临时炮塔/弹丸定义以 push + 用后 splice/pop 摘除（成对的 mk/drop、mkEntry/popEntry 助手）注入注册表，不污染后续用例；改动过出厂表字段的用例负责恢复。

**加用例约定**：按功能分组，以 `console.log('== 组名 ==')` 横幅分隔；在组内构造最小场景（优先复用 arena/mkTurret/mkEnemy 夹具），用 `run()` 推进到断言时点，一条 `check()` 一个口径（cond 只读终态，detail 带关键观测值）；涉及配置解析/口令的用例直接调 `parseConfig/applyConfig/serializeTurretDefs` 等纯函数做往返断言，素材/分层用例调 `validateArt/turretLayerSrcs/turretArtState/turretRenderKey/muzzlePos` 等纯函数，特效点用例调 `emitFortressEffects()` 传入双池并以返回的发射计数断言。新增用例不得破坏确定性（不得引入非种子随机/真实时钟依赖），且必须落在末尾汇总之前。
